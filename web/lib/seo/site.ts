// Shared site config for SEO infrastructure (app/sitemap.ts, app/robots.ts).
//
// TODO: replace with the real production domain before launch — no such
// config exists yet anywhere in this repo (checked next.config.ts and
// web/.env.example), so this is a placeholder.
export const BASE_URL = "https://fudoconsumers.com";
