import 'package:flutter/material.dart';

/// Placeholder screen for the "Mis Lugares" tab: gastronomic profile
/// powered by AI, plus per-restaurant loyalty. No business logic yet.
class MyPlacesScreen extends StatelessWidget {
  const MyPlacesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mis Lugares'),
      ),
    );
  }
}
