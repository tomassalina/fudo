# Learnings — Fudo Consumers MVP (sesión de rediseño e integración end-to-end)

Continuación de `design.md` (Decisiones 1-13, fases tempranas de planificación). Este archivo documenta las decisiones de arquitectura/producto y los aprendizajes no obvios de una sesión larga y posterior (madrugada del 2026-09-09, decenas de subagentes en paralelo, todo en `main` sin worktrees) donde se llevó Next.js y Flutter a paridad pixel-perfect con el diseño de referencia y conectados de punta a punta al backend real. Ninguna de estas decisiones estaba en `design.md` — vivían solo en mensajes de commit, en `docs/visual-qa-report.md`, en `docs/flutter-vs-nextjs-gap-report.md` y en `NEXT_STEPS_CONTEXT.md` (archivo temporal, no comiteado). Mismo formato que `design.md`: qué se decidió y por qué, incluyendo alternativas descartadas, para que cualquier agente o persona sin contexto previo no tenga que reconstruir la discusión.

## Decisión 14: `Fudo Customers.dc.html` reutiliza `Fudo App.dc.html` como layout phone (vw<900)

**Fecha:** 2026-09-08/09
**Estado:** Aceptada

**Decisión:** `Fudo Customers.dc.html` (agregado en el commit `2e46d2c`) es la referencia de diseño **wide/desktop únicamente** (vw≥900). Para vw<900 la referencia canónica sigue siendo `Fudo App.dc.html` (el diseño mobile original). El propio proyecto usa 900px como breakpoint para decidir cuál referencia mirar (confirmado en `docs/visual-qa-report.md`, sección de metodología).

**Por qué:** Se construyó primero el diseño mobile (`Fudo App.dc.html`) y luego se agregó la capa wide (`Fudo Customers.dc.html`) como una extensión, no como un rediseño completo desde cero — reduce trabajo de diseño duplicado y mantiene consistencia visual entre plataformas/anchos.

**Implicación para quien edite el diseño:** si cambiás algo del layout phone (nav, cards, tipografía base), **editalo en `Fudo App.dc.html`**, no en `Fudo Customers.dc.html` — este último solo debe llevar los agregados/diferencias específicas de vw≥900 (ej. sidebar fijo de Perfil, sidebar de filtros en Buscar, split lista/mapa). Editar el layout phone únicamente en `Fudo Customers.dc.html` lo dejaría inconsistente con la experiencia mobile real, que es la prioridad del proyecto (ver `design.md`, Decisión 10).

## Decisión 15: Auth JWT persistido en `localStorage` (no httpOnly cookie) — limitación de seguridad aceptada

**Fecha:** 2026-09-09
**Estado:** Aceptada, con tradeoff documentado en código

**Decisión:** El JWT de sesión en la web (Next.js) se persiste en `localStorage` (`web/lib/auth/token-storage.ts`), no en una cookie `httpOnly`. Implementado en el commit `0fcda09`.

**Por qué:** El backend Rails devuelve el token en el body JSON de la respuesta (sin `Set-Cookie`), y hoy no hay SSR de datos privados en la web — ambas condiciones necesarias para que una cookie `httpOnly` aporte valor real ya están ausentes. Se evaluó explícitamente el tradeoff XSS-vs-httpOnly y se aceptó `localStorage` como más simple para el estado actual del proyecto, dejando la limitación documentada en el propio código en vez de ignorarla.

**Contraste con mobile:** Flutter (`mobile/lib/core/auth/token_storage.dart`, commit `64dae2e`) guarda el JWT en `flutter_secure_storage` (Keychain/Keystore real), no en texto plano — una postura de seguridad más fuerte que la web. Esta asimetría entre plataformas es intencional (cada plataforma usó el mecanismo de storage seguro disponible más simple), no un descuido: si se refuerza la seguridad de auth en el futuro, la prioridad es la web (localStorage es el eslabón más débil), no mobile.

## Decisión 16: Fix — `dni` opcional en el registro del consumer

**Fecha:** 2026-09-09
**Estado:** Aceptada (fix, implementa `design.md` Decisión 1)

**Decisión:** `Consumer#dni` pasó de `presence: true, uniqueness: true` a `uniqueness: true, allow_nil: true`; se agregó una migración para sacar el `NOT NULL` de `dni_encrypted`/`dni_bidx` a nivel de base de datos. Commit `1cdb20e`.

**Por qué:** El registro (`POST /api/v1/registrations`) sin `dni` devolvía 422 siempre, lo cual contradecía directamente `design.md` Decisión 1: el DNI lo carga el mozo en el local al cerrar la cuenta, no el consumer al autoregistrarse en la app. Era un bug de implementación que dejaba sin efecto una decisión de negocio ya tomada y documentada. Se confirmó con `BlindIndex.generate_bidx(nil) == nil`, y que el índice único de `dni_bidx` es no-parcial (Postgres trata cada `NULL` como distinto para uniqueness, así que múltiples consumers sin DNI no colisionan).

**Gap dejado fuera de alcance a propósito:** no existe todavía un endpoint para que un mozo cargue/actualice el `dni` de un consumer después del registro (no hay recurso `consumers`/`me` en `routes.rb`). Es una limitación aceptada del MVP, misma categoría que otras ya documentadas en `VisitsController`.

## Decisión 17: Patrón de facade mock↔real — desarrollo sin bloquear en el backend

**Fecha:** 2026-09-08/09
**Estado:** Aceptada, patrón establecido en ambas plataformas cliente

**Decisión:**
- **Web:** `web/lib/data/{merchants,menu-items,business-hours,search}.ts` — cada función chequea una vez `NEXT_PUBLIC_API_BASE_URL` (`isApiConfigured()`) y elige entre `lib/mock` o la implementación real de `lib/api`. Los callers (páginas/componentes) no necesitan saber cuál respondió. Sin la env var seteada, resuelve siempre a mock de forma síncrona (comportamiento sin cambios). Commit `d446ec5`.
- **Mobile:** equivalente vía `CONNECTION_MODE` (`String.fromEnvironment`, default `local`) en `mobile/lib/data/connection_mode.dart` + `providers.dart`, que switchea `dataSourceProvider` entre `LocalDataSource` (fixtures en `mobile/assets/fixtures/*.json`) y `RemoteDataSource` (Dio contra el backend real), ambas implementando la misma interfaz abstracta `DataSource`. Commit `64dae2e`.

**Por qué:** Permite construir y probar UI en cualquiera de las dos plataformas cliente sin depender de que el backend esté corriendo o tenga el endpoint específico terminado — el swap de mock a real es una sola variable de entorno, no un rewrite. Esto fue clave para poder paralelizar decenas de subagentes trabajando en pantallas distintas de forma simultánea durante la sesión, sin bloquearse entre sí esperando que el backend avanzara. El "real" fixture shape en mobile ya coincide con `backend/db/structure.sql` desde el diseño original (ver `docs/design-brief.md`), así que el swap es literalmente de una línea.

## Decisión 18: 3 lecciones de git en working tree compartido (sin worktrees aislados)

**Fecha:** 2026-09-08/09
**Estado:** Aceptada — reglas operativas nuevas para cualquier sesión futura con subagentes en paralelo

**Contexto:** Toda la sesión corrió con decenas de subagentes en paralelo trabajando directo sobre `main`, en un único working tree compartido, sin `isolation: worktree` por agente. Esto generó 3 incidentes reales, todos resueltos sin pérdida de datos, que se convirtieron en reglas:

1. **Nunca `isolation: worktree` para agentes paralelos en este proyecto.** Un worktree separado corriendo su propio `docker compose up` sin nombre de proyecto explícito colisiona: el nombre de proyecto default de Docker Compose es el basename del directorio (`backend` en cualquier worktree), así que un `docker compose` viejo de OTRO checkout puede quedar bind-mounteado y servir código desactualizado en el mismo puerto (`:3000`), confundiendo a cualquier sesión que asuma que está pegándole al backend correcto (incidente real documentado en `docs/backend-fase3-progress-log.md`, sección "Ports"). La sesión completa optó por trabajar directo en `main` sin worktrees para evitar esta clase de problema por completo.

2. **Nunca `git stash` en un working tree compartido.** Un `git stash` accidental de un agente se llevó puesto el trabajo en curso de 3 tareas paralelas de otros agentes. Detectado por los propios agentes afectados, restaurado con `git stash pop` controlado + un merge manual en un archivo tocado por dos tareas en momentos distintos. Nada se perdió, pero fue puro trabajo de recuperación evitable (documentado en `NEXT_STEPS_CONTEXT.md`).

3. **Nunca `git commit` sin pathspec (nunca `git add .` / `git commit -a`) en un working tree compartido.** Hubo 2 incidentes de concurrencia donde un commit sin pathspec explícito se llevó puesto cambios de otra tarea en curso en el mismo working tree. Autoresueltos por los propios agentes sin pérdida de datos, pero la regla que quedó es: **siempre `git add <archivos específicos>`** para aislar qué entra en cada commit cuando hay más de un agente escribiendo al mismo tiempo.

**Por qué importa:** ninguno de los 3 incidentes perdió trabajo, pero los 3 eran evitables por completo siguiendo estas reglas desde el principio — el patrón de coordinación por mensaje entre agentes (avisar qué archivos se van a tocar) funcionó, pero no reemplaza aislar los comandos de git que puedan pisar el working tree de otro agente.

## Decisión 19: Decisiones de producto explícitas del dueño que pisan al `.dc.html` original

**Fecha:** 2026-09-09
**Estado:** Aceptada — pedido explícito posterior al diseño original, con imágenes de referencia reales

**Decisión:** Durante una ronda de correcciones visuales pedida por el dueño del producto (con capturas/imágenes de referencia reales aportadas por él, no interpretación del `.dc.html`), se aplicaron estos cambios sobre el nav inferior de la web:

- **Nav plano, sin QR elevado:** se sacó el tratamiento de FAB circular elevado que tenía el botón de QR en `PhoneNav.tsx` (que en un momento previo de la sesión se había dejado a propósito, documentado como réplica de `extendBody: true` del shell de Flutter) — los 5 ítems del nav quedan al mismo nivel, sin elevación. Commit `5e0fbd3`.
- **5to ítem (Perfil/login) siempre visible, sin sesión:** en vez de que el ítem de perfil desaparezca del nav cuando no hay sesión iniciada, ahora siempre está presente y cambia a un prompt de login (ícono + ruta `/login`) cuando el usuario no está autenticado. Mismo commit.

**Por qué:** Fue un pedido explícito del dueño del producto en una ronda de correcciones visuales en vivo (`NEXT_STEPS_CONTEXT.md`, "Ronda de correcciones visuales pedidas por el usuario"), no un bug encontrado contra el `.dc.html`. Documentado acá como decisión de producto real (no como fix de fidelidad al diseño) para que quien mire el `.dc.html` original y note la diferencia entienda que es intencional y viene de un pedido posterior, no de una desviación accidental de implementación.

**Otro cambio de producto en la misma categoría:** Home se simplificó a **hero-only, centrado**, sacando la sección "Lugares destacados" (`FeaturedGrid`) que sí estaba en el diseño original — commit `cfcf166`. Mismo criterio: decisión de producto posterior al diseño base, no una regresión.

## Decisión 20: Toggle Lugares/Platos (búsqueda cross-merchant de platos) — feature agregada en ambas plataformas

**Fecha:** 2026-09-08/09
**Estado:** Aceptada, implementada en Next.js y llevada a paridad en Flutter

**Decisión:** Se agregó un modo de resultados de búsqueda por **plato** (cross-merchant, no solo por restaurante) en Buscar, con un toggle "Lugares"/"Platos". En Flutter, esto reemplazó un placeholder explícito (`_PlatosCategory` en `filters_sheet.dart` decía literalmente "Filtro de platos disponible desde la vista de platos" como texto de feature no implementada) por una vista real (`DishResultsList`, `dish_results_list.dart`) contra `RemoteDataSource.getAllMenuItems()`.

**Por qué:** El endpoint de búsqueda de menu items ya existía en el backend real; la brecha era solo de cliente. Se priorizó llevar Flutter a paridad exacta con lo ya construido en Next.js (`ResultModeToggle.tsx`, `DishCard.tsx`) en vez de dejar mobile un ciclo por detrás — documentado en `docs/flutter-vs-nextjs-gap-report.md`, Tarea 3.

## Decisión 21: Pin card del mapa (`MerchantMapCard`) por encima del nav — elevar el layer entero, no portalear la card

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (commit `1b305e4`)

**Decisión:** `MerchantMapCard` se renderiza dentro de un `<Popup>` de `react-leaflet`, a su vez dentro del layer de mapa full-screen de `MapToggleSection.tsx` (`position: fixed`, con su propia clase `z-*`). Ese `position: fixed` + `z-index` explícito crea un **stacking context propio**: ningún z-index de lo que hay adentro (el popup de Leaflet incluido, que topea alrededor de z-index 700 internamente) puede pintar nunca por encima de un hermano de afuera con z-index mayor — como `PhoneNav` (`z-30`) — sin importar qué tan alto sea ese z-index interno. Confirmado en vivo con `orca eval`: el layer del mapa reportaba `z-index:20` de forma fija y el pin card quedaba siempre tapado por el nav flotante.

La solución elegida fue: `LeafletMap` reporta `popupopen`/`popupclose` (vía un `useMapEvents` watcher chico) hacia arriba a través de `MapPanel` hasta `MapToggleSection`, que sube el z-index de **todo el wrapper fixed del mapa** (no solo la card) a `z-[100]` mientras hay una card abierta, y lo vuelve a `z-20` al cerrarla.

**Alternativas descartadas:**
- **Portalear solo la card (`ReactDOM.createPortal` a `document.body`)**, dejando el resto del mapa en su z-20 normal. Es la solución "más limpia" en teoría (no eleva nada más que la card), pero exige reimplementar a mano el posicionamiento que Leaflet ya calcula internamente para el popup (`popupAnchor`, autoPan, seguimiento del marcador al hacer pan/zoom) — desproporcionado para el alcance de este fix.
- **Subir el z-index del wrapper del mapa de forma permanente** (no solo mientras la card está abierta). Se descartó: taparía el nav todo el tiempo que el mapa esté activo, no solo cuando hay una card abierta — contradice el comentario de diseño explícito en `use-map-mode.ts` de que "el mapa nunca debe tapar el nav" (que ahí se refiere a que el scroll-hide del nav no debe dispararse durante el pan del mapa, un problema distinto pero relacionado).

**Por qué importa para quien toque el mapa de nuevo:** cualquier otro elemento flotante nuevo que se agregue dentro del layer `fixed` de `MapToggleSection` (chips, botones, etc.) va a tener el mismo techo — ningún z-index interno lo va a sacar de ahí sin repetir este mismo patrón (levantar el wrapper entero condicionalmente) o portalear afuera del árbol.

## Decisión 22: Gotcha adicional sobre `git commit -- <pathspec>` en working tree compartido (refina Decisión 18.3)

**Fecha:** 2026-09-09
**Estado:** Aceptada — refinamiento de una regla ya vigente

**Contexto:** Durante la misma sesión larga (ver Decisión 18), un fix de `/buscar` (sacar el `<Header />` mobile, ver commit `e8385e8`) quedó sin comitear por un rato mientras otro agente trabajaba en paralelo sobre archivos distintos del mismo working tree. Ese otro agente corrió `git commit -- web/app/buscar/page.tsx <otros paths>` con pathspec explícito (siguiendo la regla de Decisión 18.3) para comitear SU propio cambio en ese mismo archivo — pero `git commit -- <pathspec>` re-stagea esos paths **desde el working tree**, no desde un índice pre-armado. Si el índice para ese archivo ya tenía algo parcial stageado a mano (ej. vía `git apply --cached` para excluir a propósito un cambio ajeno todavía sin comitear), ese staging parcial se pisa silenciosamente por el contenido completo del working tree — el `<Header />` sin comitear de la otra tarea viajó adentro de un commit con un mensaje que no lo mencionaba. Se detectó, se revirtió pensando que era screen accidental ("scope-leak"), y tuvo que volver a aplicarse una vez aclarado que era intencional (commits `ae8f8ac` → `b44087d` → `e8385e8`).

**Regla que queda, más específica que Decisión 18.3:** `git add <pathspec>` + `git commit` (dos pasos, sin `--` de pathspec en el commit) es más seguro que `git commit -- <pathspec>` cuando puede haber staging parcial en juego, porque separa explícitamente "qué quedó en el índice" de "qué hay en el working tree" — y agiliza notar un diff inesperado con `git diff --cached` antes de comitear. Y sobre todo: **no dejar cambios sin comitear por más de unos minutos en un working tree compartido**, incluso si son de un archivo que "nadie más está tocando" — otro agente puede tocar ese mismo archivo por una razón no relacionada y arrastrar el cambio ajeno sin querer.

## Decisión 23: Skeletons de `loadingMore` en el grid de escritorio (`SearchResultsGrid.tsx`) — leer `gridTemplateColumns` calculado en vez de recalcular columnas a mano

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada

**Decisión:** El grid de resultados en desktop usa `grid-cols-[repeat(auto-fit,minmax(260px,1fr))]` — la cantidad real de columnas es responsive y depende del ancho disponible, no es un número fijo conocido en JS. Para que los skeletons del `loadingMore` completen la fila incompleta (en vez de arrancar una fila nueva fija de 3, que era el bug original), la cantidad de columnas se lee directamente del browser post-layout: `getComputedStyle(gridEl).gridTemplateColumns.split(" ").length`, sobre un `ResizeObserver` en el contenedor del grid (no un listener de `resize` en `window`, porque también hay que reaccionar a cambios de layout que no disparan ese evento, p. ej. el sidebar de filtros). Con esa columna real se calcula `remainder = visibleCount % columns` y se renderizan exactamente `columns - remainder` (o `columns` si `remainder === 0`) placeholders, como **hermanos DOM de las cards reales dentro del mismo contenedor grid** (no en un `<div>` de grid separado debajo) — así el auto-placement de CSS Grid completa solo el hueco de la última fila antes de bajar a la próxima, sin que el componente tenga que decidir a mano dónde termina cada fila.

**Por qué `getComputedStyle` y no recalcular a mano:** replicar el algoritmo de `auto-fit`/`minmax` a mano (ancho del contenedor ÷ ancho de ítem + gap, con redondeo) es una segunda implementación del mismo cálculo que el browser ya resuelve de forma exacta y gratis, y que se desincroniza en los bordes (scrollbar, `box-sizing`, breakpoints intermedios no contemplados). Leer el resultado ya resuelto del layout evita esa duplicación y es la única forma de tener el número de columnas *real*, no aproximado.

**Confirmado con Playwright/CDP (1440×900, `/buscar`):** con 3 columnas reales y 10 cards visibles (`10 % 3 = 1`), aparecieron exactamente 2 skeletons a la derecha de la última card; con 2 columnas reales (viewport 1120px) y 10 cards (`10 % 2 = 0`), aparecieron exactamente 2 skeletons formando una fila nueva completa. El comportamiento de phone (`RowSkeleton`, contenedor flex separado, cantidad fija de 3) no se tocó.

## Decisión 24: Gotcha CSS — `aspect-ratio` en un ítem flex necesita `overflow-hidden` propio, no alcanza con el del contenedor (`MerchantCard.tsx`, layout `card`)

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (commit `3703399`)

**Bug reportado:** en `/buscar` desktop, las cards de merchant del grid renderizaban con alturas inconsistentes — algunas visiblemente más altas que sus hermanas en la misma fila, según la relación de aspecto natural de la foto de portada (una foto vertical-ish de comida rompía la altura de toda la card).

**Causa real (no la esperada):** el wrapper de la imagen (`<Link>` con `aspect-[16/10] w-full`) ya tenía la relación de aspecto fija desde el primer commit de esta card (`eab9471`) — no era un `aspect-ratio`/`object-cover` faltante como parecía a simple vista, y confirmado con Playwright las cards de una misma fila SÍ tenían la misma altura entre sí; el problema era fila-a-fila. La causa real: `Card` (el `<article>` contenedor) es `flex flex-col`, y ese `<Link>` es su ítem flex directo. Un ítem flex sin `min-height` explícito tiene por spec un **`min-height: auto`** que, cuando el ítem contiene un elemento reemplazado (el `<img>`, dimensionado con `h-full w-full` en porcentaje) y su propio `overflow` es `visible`, resuelve al tamaño mínimo de contenido — que para una foto usa la relación de aspecto **natural** de la imagen, no la declarada por `aspect-ratio`. Si esa altura de contenido (derivada de la imagen natural) superaba la altura calculada por `aspect-[16/10]`, el layout de flexbox estiraba el ítem (y la card entera) para acomodarla. Confirmado con Playwright midiendo el wrapper de la imagen directamente: una foto de `1200×1800` (portrait) medía `304×455px` en vez de los `304×190px` esperados por `16/10`, mientras que fotos landscape (`1200×800`) medían `304×202px` — todas midiendo, en la práctica, la altura que les daría su propia relación de aspecto natural a ese ancho, como si `aspect-ratio` no existiera.

`Card` ya tiene `overflow-hidden` en su propio className, pero eso solo recorta visualmente *después* de que el `<Link>` ya creció más allá de su caja — el `overflow` que importa para el cálculo de `min-height: auto` de un ítem flex es el del ítem mismo, no el de un ancestro.

**Fix:** agregar `overflow-hidden` al `<Link>` (el wrapper de la imagen), no solo al `<Card>`. Por spec, cuando el `overflow` de un ítem flex no es `visible`, su `min-height: auto` resuelve a `0` en vez de al tamaño mínimo de contenido — con eso `aspect-[16/10]` + `object-cover` vuelven a ser los únicos que deciden el tamaño de la caja, sin importar las dimensiones naturales de la foto. Un diff de una sola línea.

**Por qué no era una regresión:** se revisó `git log -p` de `MerchantCard.tsx` completo — la línea `aspect-[16/10]` nunca tuvo `overflow-hidden` propio desde el commit original que creó esta card; el layout `row` (mobile, caja fija `h-[88px] w-[88px]`) sí tuvo `overflow-hidden` desde siempre porque no depende de `aspect-ratio`. Era un bug latente desde el día uno, recién visible con datos mock que incluyeran una foto de relación de aspecto marcadamente distinta a 16:10 — no un fix anterior que se haya roto.

**Confirmado contra el diseño de referencia:** `docs/design-reference/Fudo Customers.dc.html` (línea 353-355, el card real de `places`, no el skeleton) usa el mismo patrón — imagen con `position: relative; aspect-ratio: 16/10` — pero el card contenedor ahí es HTML/CSS plano (`div` en flujo normal, sin `display: flex`), así que el bug de `min-height: auto` de flexbox nunca aparece en la referencia; solo se manifestó al traducir ese layout a un `Card` con `flex flex-col` en React.

**Por qué importa para quien toque más cards con imágenes de tamaño fijo:** cualquier caja con `aspect-ratio` + un `<img>`/elemento reemplazado adentro, si es (o puede llegar a ser) ítem directo de un contenedor `flex` o `grid`, necesita su propio `overflow-hidden` (o `min-h-0` explícito) — no alcanza con que el contenedor tenga `overflow-hidden`. Vale también para `MerchantMapCard` y cualquier otra card nueva que reutilice este patrón de imagen recortada.

## Nota operativa: colisión de puertos entre worktrees de Docker Compose

**Fecha:** 2026-09-08
**Estado:** Resuelta, generalizada como regla en Decisión 18.1

Un `docker compose up` sin `-p <nombre>` explícito usa el basename del directorio como nombre de proyecto — colisiona entre worktrees distintos que comparten el mismo nombre de subcarpeta (`backend/` en todos). Esto hizo que una sesión de mobile/web pegara contra un backend viejo (Fase 0/1, sin `/api/v1`) sin darse cuenta, porque el contenedor viejo de OTRO worktree seguía respondiendo en `:3000`. Documentado en detalle en `docs/backend-fase3-progress-log.md`. Regla: siempre `docker compose -p <worktree-o-proyecto>` si en algún momento se vuelve a trabajar con más de un checkout del repo al mismo tiempo.

## Decisión 25: Pill de horarios ahora es un accordion real que colapsa/expande "Horarios" — pisa la Decisión previa de "siempre expandido" y el default difiere del `.dc.html`

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (`MerchantDetailView.tsx`)

**Contexto:** `MerchantDetailView.tsx` tenía documentado explícitamente (comentario de cabecera del componente) que la lista semanal de horarios se renderizaba siempre expandida a propósito, en ambos layouts, "since this is static SSG output with no client state to back a toggle" — y el chevron de la pill de arriba ("Cerrado"/"Abierto ahora") solo hacía scroll hacia la sección "Horarios" de más abajo, sin colapsarla. El dueño de producto reportó esto como bug con una captura real: la pill parece un trigger de expand/collapse (tiene chevron) pero no correlaciona con la sección de abajo, que siempre se muestra completa.

**Decisión:** Se conectó la pill como trigger real de un accordion: un solo estado compartido (`hoursOpen`, derivado de `manualOpen ?? !isPhone` en `MerchantDetailView.tsx`) controla tanto la dirección del chevron (`expand_more`/`expand_less`) como si la sección "Horarios" completa (heading + tabla semanal) se renderiza o no. Mismo patrón `hoursOpen`/`toggleHours`/`hours.chevron` que **ya estaba especificado en ambos `.dc.html`** (`Fudo App.dc.html` y `Fudo Customers.dc.html`, buscar `toggleHours`) — la implementación previa se había desviado deliberadamente del diseño de referencia en este punto, y este pedido del dueño de producto la vuelve a alinear.

**Diferencia real con el `.dc.html`, explícita a propósito:** en ambos archivos de referencia, el estado inicial mockeado es `hoursOpen: false` sin importar el viewport (un solo objeto `state`, sin rama por ancho de pantalla). El dueño de producto pidió explícitamente que el default dependa del viewport: **abierto por defecto en desktop (>=900px)**, **cerrado por defecto en mobile (<900px)**. Esto no es una interpretación de la referencia — es un requisito de producto posterior, explícito, que prevalece sobre el default estático del prototipo (mismo criterio que la Decisión 19: pedidos explícitos del dueño con evidencia real pisan al `.dc.html` original cuando divergen).

**Cómo se resolvió el default dependiente de viewport sin quedar obsoleto tras la hidratación:** `useIsPhoneViewport()` (`lib/hooks/use-viewport.ts`) devuelve `false` (wide) como snapshot de servidor/primer paint — es la "wide es el guess más seguro" ya documentada en ese hook. Un `useState(() => !isPhone)` con inicializador perezoso solo se ejecuta una vez, en el primer render; si ese primer render todavía ve `isPhone=false` (el snapshot de servidor) y el visitante está en un viewport real de celular, el re-render de corrección de hidratación de `useSyncExternalStore` no vuelve a ejecutar el inicializador — `hoursOpen` quedaría congelado en `true` incluso en mobile. Se resolvió derivando `hoursOpen` en cada render como `manualOpen ?? !isPhone`, con `manualOpen` en `null` hasta el primer toggle manual del visitante: mientras es `null`, el valor mostrado seguí a `isPhone` en tiempo real (así se autocorrige con la hidratación); apenas el visitante togglea una vez, `manualOpen` fija el valor y ya no vuelve a seguir cambios de viewport. No hay precedente de este patrón exacto en el resto del código (`use-map-mode.ts` y otros usos de `useSyncExternalStore` en este repo no derivan un estado inicial condicional de esta forma), así que queda documentado acá para quien necesite el mismo patrón ("default por viewport que se puede overridear a mano una sola vez") en otro componente.

**Por qué no se restructuró para calzar 1:1 con el `.dc.html`:** en la referencia, la tabla semanal vive anidada *dentro* de la misma card que la pill (un solo contenedor con borde), sin un heading "HORARIOS" separado. La implementación actual tiene dos piezas visuales distintas (la pill como su propia card, y una sección "Horarios" con heading propio más abajo) — así es como el dueño de producto describió el bug y el estado deseado ("la pill de arriba expande/colapsa la sección HORARIOS de abajo"), no pidió fusionar las dos cards en una. Se mantuvo la estructura visual de dos piezas ya existente y solo se conectó el estado — cambiar el layout a una sola card hubiese sido una reestructuración visual no pedida.

## Decisión 26: `POST /api/v1/search` requería auth por accidente de diseño — la Decisión "access model" de la Decisión de autenticación (commit `8bdbbc0`) metió la pata al agrupar Search con los endpoints privados

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (fix de bug reportado en vivo por el dueño de producto)

**Bug reportado:** el prompt de búsqueda por IA del Home devolvía **401 Unauthorized** en la consola/network tab, para cualquier visitante no logueado. Reproducido real con Playwright headless (logueado afuera, `localStorage` limpio): `POST http://localhost:3000/api/v1/search` -> 401 `{"error":"Not authenticated"}`.

**Causa real:** `Api::V1::SearchController` tenía `before_action :authenticate_consumer!` desde el commit `8bdbbc0` ("feat(/backend): add JWT authentication and API rate limiting"), que documentaba explícitamente en su propio mensaje de commit la decisión de dejar `POST /api/v1/search` "fully authenticated" junto con visits/favorites/gifts/etc. — **no fue un accidente de una sola línea ni una regresión de un commit posterior** (se descartó la hipótesis inicial de que los commits `72bdc16`/`ae8f8ac`, de una tarea sobre "coordinate tags/Gemini schema", hubieran tocado esto — no lo tocaron). Fue una decisión de diseño real, tomada y documentada a propósito, que resultó estar **mal** frente a la regla de negocio real: "la busqueda por IA y /buscar es gratis, lo unico q necesita permisos es ver el perfil o regalar o ver los datos de cuantas veces fui a un restaurante" (dicho en vivo por el dueño de producto). El spec de request (`spec/requests/api/v1/search_spec.rb`) y el spec de OpenAPI/rswag (`spec/integration/api/v1/search_spec.rb`) incluso tenían un test explícito `"returns 401 without authentication"` cubriendo ese comportamiento — o sea, el comportamiento "malo" estaba testeado y verde, no era un bug no detectado.

**Fix:** se sacó el `before_action :authenticate_consumer!` de `SearchController` (`backend/app/controllers/api/v1/search_controller.rb`), siguiendo el mismo patrón que ya usan los demás endpoints públicos-para-lectura de este código (`before_action :authenticate_consumer!, except: %i[index show]` en `merchants_controller.rb`/`tags_controller.rb`/etc.) — acá simplificado a "sin auth en absoluto" porque `SearchController` solo tiene la acción `create` y es 100% pública. `SearchHistory#consumer_id` es `NOT NULL` (`belongs_to :consumer` requerido), así que `build_search_history` ahora devuelve `nil` cuando no hay `current_consumer` y el `create` no persiste ningún `SearchHistory` para un visitante anónimo — cuando SÍ hay un bearer token válido (un consumer ya logueado usando el buscador), el comportamiento de atribución a su historial sigue exactamente igual que antes.

**Por qué no era un bug de frontend:** se investigó `web/lib/api/search.ts` como candidato (el enunciado del bug sugería que quizás mandaba un header `Authorization` viejo/inválido) — pero `authHeader()` (`web/lib/auth/token-storage.ts`) devuelve `{}` cuando no hay token guardado, no manda ningún header malformado. El 401 era 100% responsabilidad del backend.

**Alternativas descartadas:** hacer `consumer_id` nullable en `search_history` para trackear también búsquedas anónimas. Se descartó por alcance — el pedido del dueño de producto fue "que no requiera login", no "que también trackeemos anónimos"; ampliar el modelo de datos para eso es una decisión de producto separada, no parte de este fix.

## Decisión 27: `AiSearchResolver.tsx` se quedaba trabado en el skeleton para siempre en dev — el guard anti-doble-llamada de StrictMode también bloqueaba el `.then`/`.catch` de la llamada real

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada

**Bug reportado (segundo síntoma del mismo incidente):** navegar directo a `/buscar?ai=cafeterias` colgaba mostrando el skeleton indefinidamente (1+ minuto sin resolver), incluso después de arreglado el 401 de la Decisión 26. Confirmado con Playwright real: el `POST /api/v1/search` completaba con **200** (visible en el network log), pero la URL nunca dejaba de tener `?ai=cafeterias` y ningún `history.replaceState`/`pushState` nuevo se disparaba — el componente quedaba renderizando `<BuscarSkeleton />` para siempre.

**Causa real:** el `useEffect` de `AiSearchResolver.tsx` usaba un solo `startedRef` para dos cosas a la vez: (1) evitar que la llamada real a Gemini (no idempotente, facturada) se disparase dos veces por el doble-invoke de efectos de React StrictMode en dev (mount → cleanup sincrónico → mount de nuevo), y (2) —sin querer— también servía de guard para *adjuntar* los handlers `.then`/`.catch`. La segunda ejecución del efecto (la que sobrevive, la "real") entraba al `if (startedRef.current) return;` y salía inmediatamente, sin registrar ni un nuevo `cancelled` ni nuevos handlers. Mientras tanto, la PRIMERA ejecución del efecto (la que StrictMode descarta) sí había disparado el fetch real y adjuntado su propio `.then`/`.catch` — pero su función de cleanup corre de inmediato (as part del doble-invoke sintético) y pone `cancelled = true` en ese closure. Cuando el fetch real finalmente resuelve (con 200), el `.then` de esa primera ejecución ve `cancelled === true` y hace `return` sin llamar a `router.replace(...)`. Resultado neto: la llamada de red se ve exitosa en el Network tab, pero nadie navega a ningún lado — exactamente lo reportado.

**Fix:** separar las dos responsabilidades. `startedRef` sigue existiendo y sigue disparando `resolveAiSearchFilters(query)` una sola vez — pero ahora la promesa en curso se guarda en un ref propio (`resultPromiseRef`), y **cada** ejecución del efecto (incluida la que sobrevive al doble-invoke) adjunta su propio `.then`/`.catch` con su propio `cancelled` local sobre esa misma promesa compartida. Así la llamada a Gemini se sigue disparando una sola vez (no se rompe el motivo original del guard), pero la ejecución del efecto que realmente queda montada sí llega a procesar el resultado y navegar.

**Por qué no se agregó una pantalla de error dedicada:** se evaluó agregar un estado de error visible (con botón de reintentar/volver) para el caso general de que la búsqueda falle o tarde — pero el flujo ya tenía manejo de fallas razonable sin necesitar una pantalla nueva: `apiFetch` (`web/lib/api/client.ts`) ya aborta con timeout a los 5s (`REQUEST_TIMEOUT_MS`), y el `.catch` de `AiSearchResolver` ya degrada con gracia a una búsqueda de texto plano (`router.replace` a `/buscar?q=<mismo texto>`) en vez de dejar al visitante colgado — un resultado de búsqueda por nombre es un resultado completo y útil, no un callejón sin salida. Se revisó `docs/design-reference/Fudo App.dc.html` buscando un patrón de pantalla de error para búsqueda fallida y no existe ninguno para este caso. Agregar una pantalla nueva sin patrón de referencia y sin necesidad real (el degrade-a-búsqueda-simple ya cumple "nunca dejar al visitante mirando un skeleton infinito" una vez arreglado el bug de arriba) hubiese sido scope no pedido.

## Decisión 28: Gotcha de tooling — correr `bundle exec rspec` dentro del contenedor Docker de `development` corre en el entorno equivocado si no se fuerza `RAILS_ENV=test`

**Fecha:** 2026-09-09
**Estado:** Aceptada — gotcha documentado, no requirió cambio de código

**Síntoma:** al verificar el fix de la Decisión 26 corriendo `docker compose exec -T web bundle exec rspec spec/requests/api/v1/search_spec.rb`, **todos** los specs de request fallaban con `403 Forbidden` — incluidos specs de endpoints públicos sin tocar en esta tarea (`merchants_spec.rb`, `"requires no authentication — merchant discovery is public"`). El body de la respuesta era la página de error nativa de Rails `ActionDispatch::HostAuthorization`: `"Blocked hosts: www.example.com"` (el host default que usa el cliente de test de `rspec-rails` en sus requests).

**Causa real:** el contenedor `backend-web-1` corre con `RAILS_ENV=development` seteado por `docker-compose.yml`. `spec/rails_helper.rb` (generado por Rails, no tocado en este repo) hace `ENV["RAILS_ENV"] ||= "test"` — el `||=` no pisa un valor ya seteado, así que **rspec corría contra el entorno `development`**, no `test`. `config.hosts` en `development` trae por default `[".localhost", ".test", IPAddr 0.0.0.0/0, IPAddr ::/0]` (vacío en `test`, sin restricción) — ninguno de esos patterns matchea `www.example.com`, el host default de los request specs, así que `ActionDispatch::HostAuthorization` bloqueaba el 100% de las requests con 403 antes de que llegaran a ningún controller. Confirmado imprimiendo `Rails.application.config.hosts` desde dentro de un spec real (development defaults) vs. desde `bin/rails runner` con `RAILS_ENV=test` explícito (`[]`, vacío).

**No es una regresión de esta tarea ni de ninguna otra reciente** — es un gotcha del entorno Docker de este repo en sí, expuesto recién ahora porque es la primera vez en esta sesión que se corrió `bundle exec rspec` a mano dentro de `docker compose exec web` en vez de vía CI o `docker compose run` (que sí podría tener `RAILS_ENV` distinto). Se confirmó forzando `RAILS_ENV=test` explícito (`docker compose exec -T -e RAILS_ENV=test web bundle exec rspec`): con eso, la suite completa (402 examples) corre y pasa en verde.

**Regla que queda para quien corra tests dentro de este contenedor de dev:** siempre `docker compose exec -T -e RAILS_ENV=test web bundle exec rspec ...` (o exportar `RAILS_ENV=test` en el shell del `exec`) — nunca asumir que el contenedor de `docker compose up` normal corre los tests en el entorno correcto solo porque `rails_helper.rb` "ya se encarga". No se tocó `docker-compose.yml` ni `rails_helper.rb` para forzar esto porque cambiar el `RAILS_ENV` default del contenedor rompería el flujo normal de `rails server` en dev, y agregar un `ENV["RAILS_ENV"] = "test"` (sin `||=`) a `rails_helper.rb` para "arreglarlo" del lado del repo escondería el mismo problema para cualquiera que corra rspec fuera de Docker con un `RAILS_ENV` real distinto seteado a propósito (ej. una entorno de staging local) — mejor un hábito documentado que una sobre-corrección silenciosa en un archivo generado por el framework.

## Decisión 29: `CardSkeleton.tsx` como componente compartido propio en vez de exportarlo desde `SearchResultsGrid.tsx`

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (commit `937258c`)

**Contexto:** los skeletons del grid de escritorio en `/buscar` (tanto el loading inicial de `BuscarSkeleton.tsx` como el "loading more" de `SearchResultsGrid.tsx`, corregido en conteo por la Decisión 23) renderizaban un bloque plano `h-[240px] animate-pulse` sin estructura interna ni shimmer, y con una altura hardcodeada que quedó desactualizada apenas la Decisión 24 fijó la altura real de `MerchantCard` vía `aspect-[16/10]` en vez de un alto fijo.

**Decisión tomada:** crear `web/components/features/buscar/CardSkeleton.tsx` como archivo propio (siguiendo el patrón de un componente por archivo que ya usan `MerchantCard.tsx`, `DishCard.tsx`, `FavoriteButton.tsx`), en vez de exportar la función desde `SearchResultsGrid.tsx` e importarla en `BuscarSkeleton.tsx`. `CardSkeleton` reusa el mismo `aspect-[16/10]` que la card real (así la altura queda atada a la misma relación de aspecto en vez de un número hardcodeado que puede volver a desincronizarse) y el mismo primitivo `Card` (`components/ui/Card.tsx`) que usa `MerchantCard` vía `<Card as="article">`, más el shimmer `animate-fudo-shimmer` que ya usaba `RowSkeleton()` (mobile) — reusado tal cual, no reinventado.

**Alternativa descartada:** exportar `CardSkeleton` desde `SearchResultsGrid.tsx` (que ya tenía `RowSkeleton()` como función interna) e importarlo desde ahí en `BuscarSkeleton.tsx`. Se descartó porque `SearchResultsGrid.tsx` es un client component grande con lógica de negocio (fetch de resultados, scroll infinito, estado de filtros) — hacer que `BuscarSkeleton.tsx` (que solo necesita un componente presentacional puro para el loading state inicial de la ruta) dependa de ese archivo entero invierte la dirección de dependencia esperada: un componente de loading no debería importar de un componente interactivo con lógica propia solo para tomar prestada una función presentacional.

**Gotcha encontrado en la primera versión (corregido en el mismo commit tras code review):** la primera versión de `CardSkeleton` copió a mano las clases del shell de `Card` (`rounded-card border border-border bg-surface shadow-[inset_0_1px_0_var(--highlight)]`) en vez de usar el componente `Card` directamente — duplicación exacta, byte a byte, de lo que `components/ui/Card.tsx` ya encapsula. Si el shell de `Card` cambia (radio, color, highlight), el skeleton quedaría desincronizado en silencio. Fix: `<Card aria-hidden className="flex flex-col overflow-hidden">` en vez del `<div>` con las clases copiadas.

**Confirmado contra el diseño de referencia:** `docs/design-reference/Fudo Customers.dc.html` (líneas 416-431, bloque `skeletons` del grid `isList`) especifica exactamente esta estructura — `border-radius:20px` (`rounded-card`), caja de imagen `aspect-ratio:16/10` con el mismo shimmer `fudoShimmer`, `padding:16px` (`p-4`), `gap:10px` (`gap-2.5`), y tres barras de texto `16px/60%`, `12px/45%`, `12px/30%` — usado como fuente para las proporciones de las barras en vez de estimarlas a ojo contra el texto real de `MerchantCard`.

**No se tocó** `RowSkeleton()` (mobile) ni la lógica de conteo de columnas de `SearchResultsGrid.tsx` de la Decisión 23 — solo cambió qué apariencia tiene cada placeholder, no cuántos se renderizan ni dónde.

## Decisión 30: Schema de Gemini ganó `query`/`result_mode` para cerrar el gap de "quiero comer huevo" / "busco la parrilla de Juan" — el parser sigue siendo SOLO un extractor de filtros, nunca hace matching de texto él mismo

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada

**Problema:** `SearchQueryParser.response_schema` (`backend/app/services/search_query_parser.rb`) no tenía ningún campo para texto libre — una consulta que nombra un plato/ingrediente específico ("quiero comer huevo") o un comercio específico ("busco la parrilla de Borges") no mapea a ningún enum fijo de `type`/`tags`, así que cualquiera de esas dos formas de búsqueda caía sin ningún filtro derivado, y el resultado final en `/buscar` terminaba siendo "todos los comercios" en vez de acercarse a lo que la persona pidió.

**Decisión tomada:** se agregaron dos campos ADITIVOS al schema de respuesta de Gemini (sin tocar `neighborhood`/`type`/`tags`/`price_per_person`/`open`/`reward`, que quedan exactamente igual):
- `query` (STRING nullable): el fragmento de texto libre que Gemini extrae cuando la consulta nombra algo específico que no cae en los enums existentes.
- `result_mode` (STRING nullable, enum `["lugares", "platos"]`): si esa consulta es sobre un LUGAR o un PLATO.

Ambos se mapean 1:1 a los params que `/buscar` ya entendía de antes (`q` y `mode`, ver `web/lib/utils/buscar-href.ts`) — el parser de Gemini **nunca hace matching de nombre/plato él mismo**, solo devuelve el fragmento; quien realmente compara ese texto contra `merchant.name`/`item.name`/`item.description`/`item.section` es la barra de búsqueda propia de `/buscar` (`getDishSearchResults` en `web/lib/data/menu-items.ts`, ya existente, no tocada). Esta separación de responsabilidades es una regla existente del proyecto (ver el comment-header del archivo) y esta tarea la respeta explícitamente en vez de relitigarla.

**Cadena de wiring del lado del cliente (los 3 puntos que había que tocar, no solo 1):** el pedido original solo mencionaba `web/lib/search/resolve-ai-search.ts`, pero mapear el campo ahí NO alcanzaba por sí solo — `ResolvedAiFilters` es un objeto intermedio que solo tiene efecto si su consumidor lo usa. Se encontraron y corrigieron 3 archivos en cadena:
1. `web/lib/api/search.ts` — el tipo `SearchQueryFilters` (espejo campo-a-campo del schema del backend, según su propio comentario) necesitaba los dos campos nuevos primero.
2. `web/lib/search/resolve-ai-search.ts` — mapea `filters.query -> q` y `filters.result_mode -> mode` (con `"lugares"` colapsado a `null`, nunca al literal `"lugares"` — mismo criterio que ya usa `ResultModeToggle.tsx` para su propio href, donde `mode: m.key === "lugares" ? null : m.key`).
3. `web/components/features/buscar/AiSearchResolver.tsx` — **este era el punto que realmente faltaba**: el `router.replace(buscarHref(...))` del caso de éxito nunca incluía `q`/`mode` en sus overrides (solo `type`/`hood`/`tags`/`price`/`open`/`reward`) — sin este cambio, el mapeo del paso 2 quedaba calculado pero descartado, el bug seguía sin cerrarse de punta a punta.

**Corrección de un dato asumido incorrecto en el pedido original:** el pedido decía textualmente que el comentario existente en `search_query_parser.rb` explica `dist`/`sort`/`hideVisited` como "deliberately excluded". Se verificó contra el archivo real antes de tocar nada (regla del propio pedido: "CONFIRM against the actual file, don't trust this list") y el comentario real decía `dist`, `sort`, **y `mode`** — no `hideVisited` (que ni siquiera aparece mencionado en ese comment-header; nunca tuvo semántica en el schema del parser porque no es un filtro que pueda derivarse de texto libre, análogo a por qué el pedido tampoco lo requería). Como `mode` es exactamente el campo que esta tarea agrega (vía `result_mode`), el comment-header se actualizó para sacar `mode` de la lista de "deliberately NOT covered" y dejar únicamente `dist`/`sort` (con su razonamiento original intacto, sin tocar), documentando en su lugar por qué `query`/`result_mode` sí se agregan ahora.

**Nombre elegido para el enum (`result_mode`, no `mode`):** se usó `result_mode` en vez de `mode` a secas para el campo del schema de Gemini, para no generar ambigüedad con el propio `mode` de `BuscarParams` en el nombre del campo devuelto por la IA (son conceptualmente el mismo valor, pero viven en capas distintas — uno es la salida estructurada de un LLM, el otro es un param de URL). El mapeo 1:1 de valores (`"lugares"`/`"platos"`) se mantiene idéntico a `ResultMode` (`web/components/features/buscar/ResultModeToggle.tsx`).

**Alcance de los tests actualizados (además del código):** se extendieron `backend/spec/requests/api/v1/search_spec.rb` (2 queries reales nuevas contra Gemini: `"quiero comer milanesa"` y `"busco la parrilla de Borges"`, más aserciones de shape para `query`/`result_mode` en las queries ya existentes), `backend/spec/integration/api/v1/search_spec.rb` (doc de OpenAPI/rswag + los hashes stubbeados de `SearchQueryParser.call`, para que seguir documentando el shape completo no quede desactualizado), y del lado del frontend `web/__tests__/lib/search/resolve-ai-search.test.ts` y `web/__tests__/components/features/buscar/AiSearchResolver.test.tsx` (incluyendo el caso `result_mode: "lugares" -> mode: null`). Un test viejo de `AiSearchResolver.test.tsx` afirmaba literalmente "Never a `q`/`ai` param on success" — ese comportamiento cambió a propósito (ahora si Gemini extrae `query` sí se propaga `q` en el caso de éxito), así que el test se corrigió para reflejar el nuevo contrato en vez de mantenerse verde por casualidad (el mock que usaba no incluía `q`/`mode`, por lo que hubiese quedado en un falso positivo).

**Datos seed reales usados para los ejemplos/tests (verificados contra `backend/db/seeds.rb`, no inventados):** plato con ingrediente distintivo → `"Milanesa napolitana con papas fritas"` (pool `DISH_POOLS["restaurant"]`); comercio real → `"La Parrilla de Borges"` (`MERCHANTS_DATA`). No existe ningún plato seedeado que contenga literalmente la palabra "huevo" — el "quiero comer huevo" del pedido original era solo un ejemplo ilustrativo, no un dato real a verificar contra los seeds.

**Verificación:** suite completa de backend (`bundle exec rspec`, dentro del contenedor Docker con `RAILS_ENV=test` explícito por el gotcha de la Decisión 28) → 404 examples, 0 failures, incluyendo llamadas reales (no mockeadas) a Gemini para las 2 queries nuevas. Suite completa de frontend (`pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test`) → sin errores de tipos, sin warnings de lint, 207 tests en 36 archivos, todos verdes.

## Decisión 31: Split de la tab de fidelización en 2 sub-columnas para desktop (`LoyaltyHeroCard`/`LoyaltyRoadmap` extraídos), gate logueado/no-logueado sin tocar

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada

**Contexto:** `MerchantDetailView.tsx` (rama `!isPhone`, agregada en un commit reciente `453712c` por otra sesión) ya tenía la grilla de 2 columnas correcta a nivel de página (rail izquierdo sticky con horarios/precio/contacto + columna ancha con las tabs), pero `LoyaltyCard.tsx` — lo que renderiza esa columna ancha en la tab "Mis visitas" — seguía siendo una única columna vertical (hero card → roadmap → nota QR) en *ambos* tamaños de viewport. El diseño de referencia (`docs/design-reference/Fudo Customers.dc.html`, línea ~586, `grid-template-columns: repeat(auto-fit, minmax(330px, 1fr))`) especifica que en desktop esto se parte en 2 sub-columnas lado a lado: `[hero card + nota QR]` a la izquierda, `[label "TU CAMINO EN X" + roadmap numerado]` a la derecha — dando la apariencia final de layout de 3 columnas que pedía la tarea.

**Decisión tomada:**
1. Se extrajeron `LoyaltyHeroCard.tsx` y `LoyaltyRoadmap.tsx` (nuevos, `web/components/features/restaurantes/`) desde el JSX que antes vivía inline en `LoyaltyCard.tsx`, cada uno con un prop `size: "phone" | "wide"` que aplica los valores exactos de cada `.dc.html` de referencia (ring 78px→92px, padding 20px→24px, texto de premio 13.5px→14px, badges 9.5px→10px, etc. — ver comentarios de cabecera de ambos archivos para la lista completa).
2. `LoyaltyCard.tsx` ahora lee `useIsPhoneViewport()` (mismo hook/breakpoint de 900px que ya usa `MerchantDetailView.tsx`) y solo activa el grid de 2 sub-columnas (`repeat(auto-fit, minmax(330px, 1fr))`, gap 22px — valores literales del `.dc.html`) cuando `isAuthenticated && !isPhone`. Todo lo demás (phone en cualquier estado de auth, y wide-pero-no-logueado) sigue siendo la columna única que ya existía.
3. El gate logueado/no-logueado (CTA con candado "Recompensas" en vez del roadmap) **no se tocó** — se mantuvo idéntico en ambos tamaños. Esto es una desviación deliberada respecto de la referencia: ninguno de los dos `.dc.html` gatea esta sección (su `loyaltyOf()` mock renderiza el mismo roadmap a 0 visitas con copy genérico para un visitante no logueado) — pero la tarea pedía explícitamente "mirror what mobile already does logged-out" en vez de adoptar el comportamiento del mock, así que se preservó el gate real ya existente (decisión de una sesión anterior, no de esta tarea) sin importar el viewport.

**Alternativa descartada:** usar el grid `auto-fit` de forma puramente CSS (sin branch de JS) para que colapse a 1 columna en angosto por sí solo. Se descartó porque el ORDEN de los elementos difiere entre phone y wide, no solo el número de columnas — en phone la nota QR va *después* del roadmap (al final de todo), en wide va *junto al hero card* (antes, en la sub-columna izquierda) — un reflow CSS puro no puede reordenar así sin `order`/`grid-area` por breakpoint, y el proyecto ya tiene un precedente establecido (`useIsPhoneViewport`) para exactamente este caso: estructura genuinamente distinta, no solo reflow.

**Gap de datos de backend confirmado (no introducido por esta tarea, ya preexistía):** el `TIERS_L` del `.dc.html` (min visitas → "NIVEL BRONCE"/"PLATA"/"ORO"/"CLIENTE FIJO", con gradientes de ring por tier) es 100% mock — `buildLoyaltyProgress` (`web/lib/data/loyalty.ts`) no tiene ningún concepto de tier con nombre; devuelve `tierLabel: "TU CAMINO"` fijo para cualquier logueado, sin importar la cantidad de visitas. Confirmado revisando el archivo completo: no hay ningún campo de "tier" en `LoyaltyRule`/`LoyaltyProgress` (`lib/types/index.ts`) ni en el serializer de `loyalty_rules` del backend. No se inventó un sistema de tiers nuevo para esta tarea — se mantuvo `progress.tierLabel` tal cual ya lo devolvía `buildLoyaltyProgress` (decisión de una sesión anterior). Si en algún momento se quiere el "NIVEL ORO" real del diseño, hace falta primero decidir de qué campo de negocio sale un nombre de tier (¿un `tier` explícito en `loyalty_rules`? ¿derivado de qué regla se alcanzó?) — no hay ninguna fuente de verdad de backend para eso hoy.

**Botones de contacto (WhatsApp/Delivery) y nota QR — valores de tamaño desktop reales, no reusados de phone:** `contactButtons()` en `MerchantDetailView.tsx` pasó de un parámetro `fullWidth: boolean` a `variant: "phone" | "wide"` porque los dos `.dc.html` especifican tamaños distintos para el mismo botón (13.5px/ícono 17px/gap 7px en wide vs. 12.5px/ícono 16px/gap 6px en phone, confirmado línea por línea contra ambos archivos) — antes de este cambio, el desktop reusaba silenciosamente los valores tuneados para phone (mismo `fullWidth` solo cambiaba `flex-1`, no tipografía). Mismo criterio aplicado a la nota QR dentro de `LoyaltyCard.tsx` (12.5px/ícono 20px en phone vs. 13px/ícono 21px en wide).

**Verificación:** `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test` (207/207, incluyendo `LoyaltyCard.test.tsx` sin modificar — corre en jsdom con `innerWidth` default 1024px, por lo que ya ejercitaba la rama `wide` sin necesidad de mockear el viewport) y `pnpm build` — los 4 en verde. Playwright ad hoc (`npx playwright`, no es dependencia del proyecto) contra el merchant real `id=1788` ("Almacén Malabia", 5 visitas reales para `info@tomassalina.com`, ladder hasta 10) a 1440×900 logueado y no logueado, y 390×844 logueado — capturas en el scratchpad de la sesión. Valores computados verificados: label "PRECIO POR PERSONA" → 11px/700/letter-spacing 1.1px; precio → 20px/800/Barlow; badge "ESTÁS ACÁ" → 10px/700, fondo `rgba(143,212,106,0.14)`.

## Decisión 32: Gotcha de fuentes — la sintaxis `@valor,valor,valor,valor` (punto fijo) de la API CSS2 de Google Fonts descarga una instancia ESTÁTICA, no la fuente variable

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — fix de bug reportado por el dueño de producto

**Síntoma reportado:** el ícono de favorito (corazón) en las cards de comercio no se veía sólido/relleno en rojo cuando el comercio estaba marcado como favorito — solo el contorno/trazo se veía en rojo, el interior quedaba hueco.

**Causa raíz confirmada:** `web/app/layout.tsx` cargaba Material Symbols con `https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,200,0,0&display=swap`. La API CSS2 de Google Fonts interpreta `@valor,valor,valor,valor` (números fijos separados por coma, sin `..`) como un pedido de instancia **estática** congelada en esos ejes exactos (`opsz=24, wght=200, FILL=0, GRAD=0`) — no la fuente variable completa. Confirmado empíricamente pidiendo la misma URL con curl (User-Agent de Chrome moderno) y comparando la respuesta: la versión de punto fijo devuelve `font-weight: 200;` (un solo valor) y sirve un `.woff2` distinto al de la versión con rangos, que devuelve `font-weight: 100 700;` (una fuente variable real, con eje wght declarado como rango). Como consecuencia, cualquier componente que seteaba `fontVariationSettings` con `'FILL' 1` inline (`FavoriteButton.tsx`, `FavoritesTab.tsx`) no tenía ningún efecto visual — el archivo de fuente cargado ni siquiera contenía el glyph relleno, solo el outline. La parte `color: "#FF5023"` del mismo `style` inline sí funcionaba (es CSS estándar, no depende del archivo de fuente), lo que explica exactamente el síntoma "contorno rojo, interior hueco".

**Fix:** cambiar la URL a la sintaxis de rango (`min..max` por eje) que la propia documentación/generador de Google Fonts usa para Material Symbols: `opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200`. Con esto la hoja de estilos servida es una fuente variable real, y los `fontVariationSettings` inline (tanto el `FILL` de favoritos como los `wght` no-default de `SearchBar.tsx`/`SortMenu.tsx`, que también estaban silenciosamente rotos por el mismo bug) vuelven a tener efecto.

**Otros usos afectados por el mismo bug, encontrados por grep de `fontVariationSettings`/`font-variation-settings` en `web/components` y `web/app`:** `SearchBar.tsx` (dos instancias con `'wght' 250`/`'wght' 300`) y `SortMenu.tsx` (`'wght' 300`) — ningún otro componente usa un valor de eje distinto al default (`FILL 0, wght 200, GRAD 0, opsz 24` declarado en `.material-symbols` de `globals.css`, que se dejó sin tocar a propósito). El nuevo rango del `wght` (100..700) cubre los tres puntos usados (200/250/300) sin necesidad de ajustar nada más.

**Verificación:** `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test` (208/208) y `pnpm build` — los 4 en verde. Playwright ad hoc (`npx playwright`, headless Chromium, no es dependencia del proyecto) logueado como `info@tomassalina.com`/`Demo1234` (favoritos reales seedeados: Almacén Malabia, Che Bo Parrilla, La Cocina de Mateo) — capturas de `/perfil` → tab Favoritos y de `/buscar` muestran el corazón sólido/relleno en rojo para los 3 comercios favoritos y el contorno hueco sin rellenar para comercios no favoritos en la misma vista. Confirmado además vía red: el `<link>` servido usa la nueva URL con rangos y el CSS devuelto por `fetch` en el propio navegador tiene `font-weight: 100 700` (no un valor fijo), y `getComputedStyle(...).fontVariationSettings` del corazón favorito muestra `"FILL" 1, ... "wght" 250`.

## Decisión 33: `Merchant.search` matcheaba `neighborhood` con exact-match case-sensitive — filtro que puede llegar en cualquier casing tiene que normalizarse en el query, no en cada caller

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — fix de bug reportado en vivo por el dueño de producto, con evidencia real

**Síntoma reportado:** `/buscar?tags=sin_tacc&hood=palermo&...` (minúscula) devolvía CERO resultados, mientras que `?hood=Palermo` (mayúscula, igual al casing exacto guardado en la DB) funcionaba bien.

**Causa raíz confirmada:** `Merchant.search` (`backend/app/models/merchant.rb:54`, antes del fix) hacía `scope.where(neighborhood: neighborhood)` — un exact match SQL, case-sensitive, contra `neighborhood` (`character varying(100)`, sin `citext`, con un índice btree plano — confirmado leyendo `backend/db/structure.sql`, no hay ningún precedente de `citext` en todo el schema). Cualquier casing que no coincidiera EXACTO con lo guardado en la fila devolvía cero filas, silenciosamente (sin error, sin warning).

**Por qué es un gotcha arquitectónico y no un bug puntual de un caller:** `neighborhood` puede llegar con casing no confiable desde más de una fuente: (1) la extracción de Gemini (`SearchQueryParser`), cuyo `SYSTEM_INSTRUCTION` le pide explícitamente devolver el texto "exactly as written by the user" — sin garantía de que el usuario haya escrito "Palermo" con mayúscula; (2) un param de URL tipeado a mano (`?hood=palermo`); (3) a futuro, cualquier fuente de seed data con una convención de casing distinta. El fix correcto vive en `Merchant.search` mismo, no en cada caller — mismo criterio que ya existía en este archivo para `tags` (ver `filter_by_tags`, que ya hacía `LOWER(tags.name) IN (?)` con el mismo razonamiento, con comentario explícito sobre el casing no garantizado de Gemini). Se siguió ese mismo patrón exacto para `neighborhood` (`filter_by_neighborhood`, `LOWER(neighborhood) = ?` con el input downcaseado) — no se usó `citext` porque no hay ningún precedente de esa extensión en el schema, y agregarla ahora habría sido una convención nueva no pedida, contra una migración de columna que no hacía falta para resolver el bug.

**`type` NO tiene el mismo riesgo — verificado, no asumido:** `type` es un enum nativo de Postgres (`merchant_type_enum`) con keys fijas en minúscula (`enum :type, {...}` en el modelo). Ambos callers ya garantizan un valor normalizado antes de llegar a `.search`: `Api::V1::MerchantsController#valid_type_filter?` valida `Merchant.types.key?(params[:type])` antes de filtrar, y `SearchQueryParser.response_schema` restringe el `type` que puede devolver Gemini a `Merchant.types.keys` (schema `enum` constraint). Se agregó un spec (`spec/models/merchant_search_spec.rb`, "type matching") que confirma que un valor no normalizado (`"RESTAURANT"`) ni siquiera llega a matchear silencioso — Postgres lo rechaza con `PG::InvalidTextRepresentation` porque el enum es un tipo nativo de columna, no texto libre. No se tocó el filtro de `type`.

**Se evaluó pero se descartó normalizar casing también en `SearchQueryParser` (defensa en profundidad):** el fix de `Merchant.search` ya cubre las 3 fuentes de casing no confiable mencionadas arriba, así que una normalización adicional en el parser sería redundante para el bug en sí. Se descartó activamente (no por omisión) porque `structured_output["neighborhood"]` no solo alimenta `Merchant.search` — también se devuelve tal cual en la respuesta de `POST /api/v1/search` como `filters.neighborhood` (`search_controller.rb`), que el cliente usa para armar la URL compartible `/buscar?hood=...` (ver comentario en `search_controller.rb`). Forzar un casing ahí (ej. downcase) cambiaría ese valor devuelto sin necesidad, con riesgo de romper cualquier lugar que hoy asuma que `filters.neighborhood` refleja el texto tal como lo escribió el usuario (el propio `SYSTEM_INSTRUCTION` lo pide así a propósito). Mejor dejar esa capa intacta y resolver el bug en la única capa que real y necesariamente lo requiere.

**Regresión agregada:** `backend/spec/models/merchant_search_spec.rb` (nuevo) — `Merchant.search(neighborhood: "palermo")` vs. `Merchant.search(neighborhood: "Palermo")` devuelven los mismos merchants (comparado explícitamente contra IDs creados en el spec, no contra el resultado completo sin acotar, porque `db/seeds.rb` ya siembra un merchant "Palermo" real que hubiera hecho pasar el test aunque el fix estuviera roto para casing distinto al de seeds). También variantes `PALERMO`/`pAlErMo`, un neighborhood inexistente, y el caso `blank?`. Más un test de request en `backend/spec/requests/api/v1/merchants_spec.rb` ("matches neighborhood case-insensitively") contra `GET /api/v1/merchants`, que es el endpoint real que pega el sidebar de filtros de `/buscar` (no el de `POST /api/v1/search`, que es el de Gemini — confirmado leyendo ambos controllers).

**Verificación:** `docker compose exec -T -e RAILS_ENV=test web bundle exec rspec` (ver Decisión 28 sobre por qué `-e RAILS_ENV=test` es obligatorio en este contenedor) → 411 examples, 0 failures. Curl real contra el server de dev corriendo (`docker compose up`): `GET /api/v1/merchants?neighborhood=palermo&per_page=5` y `GET /api/v1/merchants?neighborhood=Palermo&per_page=5` devuelven el mismo JSON byte a byte (`total_count: 30`, mismos 5 merchants en el mismo orden) — antes del fix, la primera request devolvía `data: []`.

## Decisión 34: `/buscar` mobile — mapa full-bleed con header flotante, fix del reload real en la búsqueda, `tab=map` en la URL, y marcador de ubicación propia

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — fix de 3 bugs de UX reportados por el dueño de producto en la vista de mapa mobile de `/buscar`, más un agregado de scope (marcador "estás acá") pedido a mitad de tarea.

**Diagnóstico real, no asumido — dos mecanismos distintos causaban síntomas parecidos:**
1. **Reload duro confirmado:** `SearchBar.tsx` era un `<form action="/buscar" method="GET">` nativo sin `onSubmit`/interceptación — apretar Enter disparaba una navegación real de documento completo (no un soft-nav de Next.js), que destruye y recrea todo el árbol de React, incluida la instancia de `L.Map` de Leaflet. Confirmado con una prueba de Playwright (listener de `beforeunload`) antes de tocar código, no asumido por lectura de fuente.
2. **El filtro (`PhoneFilterSheet`'s `router.push`) NO remonta el mapa** — se trazó el árbol completo desde `page.tsx` hasta `LeafletMap.tsx` y no hay ningún `key=` derivado de `searchParams` en ningún ancestro del mapa (el único `key` existente está en `SearchResultsGrid`, un hermano, no un ancestro). Confirmado además en runtime con una prueba de Playwright que estampa el nodo DOM del `.leaflet-container` y verifica que sobrevive a una navegación soft-nav same-route. El Suspense fallback de `app/buscar/loading.tsx` tampoco se re-dispara en el path de datos mock (síncrono) actual — queda pendiente re-verificar esto el día que el backend real introduzca latencia async genuina, algo que el path mock no puede ejercitar.

**Fix de layout full-bleed — por qué el fix vive adentro del propio layer `fixed`, no en un ajuste de z-index del lado in-flow:** se verificó en vivo (Playwright) que un layer `position: fixed` siempre gana la pelea de stacking contra un hermano in-flow sin importar el orden del DOM o el z-index que se le ponga al lado in-flow. La única forma correcta de que la search bar/toggle floten sobre el mapa es renderizarlos *adentro* del mismo stacking context fijo (prop `overlay` pasado a `MapToggleSection`, posicionado `absolute` dentro de ese layer) — lo que además es lo que permite que el mapa llegue a `inset-0` real sin el offset `top-20` hardcodeado que tenía antes. Se preservó intacto el esquema de z-index de `1b305e4` (`z-30` pill / `z-20` base / `z-[100]` con popup abierto) y se confirmó que `Header.tsx` sigue sin renderizarse en `/buscar` mobile.

**Decisión push vs. replace — diverge del criterio sugerido inicialmente en el pedido, a favor de la convención ya existente del código:** se relevó el código real (`PhoneFilterSheet`/`FilterSidebar`'s "Aplicar" y `SortMenu` ya usaban `router.push`, mientras que el único `replace` existente era el sync de lat/lng, un efecto de background, no una acción del usuario) y se decidió: **la búsqueda usa `push`** (una acción explícita de "Enter" que cambia contenido, igual que aplicar un filtro — el usuario espera que el botón Atrás la deshaga), **el toggle `tab` del mapa usa `replace`** (un toggle de vista sobre el mismo resultset, mismo criterio que el sync de lat/lng — abrir/cerrar el mapa repetidas veces no debería ensuciar el historial). Documentado en los comentarios de cabecera de `SearchBar.tsx` y `BuscarView.tsx`.

**Bug real encontrado en la revisión adversarial post-implementación, corregido antes de commitear — `showMap` como `useState` inicializado una sola vez se desincroniza del `tab` de la URL:** la primera versión implementada usaba `const [showMap, setShowMap] = useState(current.tab === "map")`, que solo lee `current.tab` en el mount inicial. Como `BuscarView` deliberadamente **nunca se remonta** entre navegaciones `router.push`/`replace` en `/buscar` (sin eso no sobreviviría el pan/zoom del mapa — es la base del fix del punto 2), ese inicializador nunca vuelve a correr: si el visitante abre el mapa (`tab=map`, vía `replace`, que pisa la entrada de historial en el lugar) y después aprieta **Atrás** del navegador a una entrada anterior sin `tab`, `current.tab` cambia (nuevo prop del server) pero el `useState` local queda con el valor viejo (`true`) — la URL dice lista, la UI sigue mostrando el mapa. Reproducido con una prueba de Playwright dedicada (navegar, filtrar, abrir mapa, Atrás) antes de aplicar el fix, y de nuevo después para confirmar que ya no ocurre. **Fix elegido:** en vez de agregar un `useEffect` con `setState` (que además dispara un error de lint real del proyecto, `react-hooks/set-state-in-effect`, por el riesgo de renders en cascada), se eliminó el `useState` por completo y `showMap` se **deriva directamente** de `current.tab === "map"` en cada render — mismo patrón que ya usaba `mode` en el mismo archivo (derivado de `current.mode`, sin estado local, sin este bug). Como `router.replace`/`push` en App Router siempre produce un nuevo render con el `current` actualizado, no hace falta estado local ni para el caso "optimista" del click — el toggle simplemente llama `router.replace(...)` y el próximo render ya trae el `showMap` correcto.

**Marcador "estás acá" — prop dedicado, no un `Merchant` sintético:** se agregó `userLocation?: Coordinates | null` a `LeafletMapProps`/`MapPanelProps` (y threadeado a través de `MapToggleSection` para mobile y `DesktopMapSplit` para desktop, ya que `LeafletMap.tsx` es la única implementación de Leaflet compartida entre ambos — único import site confirmado por grep). Se descartó meter la ubicación del usuario como una entrada más de `merchants: Merchant[]`: `Merchant` (`lib/types`) tiene varios campos requeridos (`id`, `name`, `type`, `address`, `country`, `state`, `city`, `tags`, `distanceKm`) que una entrada sintética tendría que rellenar con valores sin sentido de negocio, y además heredaría el ícono de `createMerchantIcon` (badge por `merchant.type`) para algo que no es un comercio. El marcador consume pasivamente `coords` de `useLocation()` (el mismo hook que ya usa `LocationButton.tsx`/`use-merchant-distance.ts`) — **nunca llama `requestLocation()` desde el mapa**, para no disparar un segundo prompt de permiso del navegador; si `coords` es `null` (idle/denegado/no soportado) simplemente no se renderiza el marcador, sin fallback falso ni crash, siguiendo el mismo criterio binario que ya usa `LocationButton.tsx` (`active = coords !== null`, sin UI distinta por status). Estilo: `L.divIcon` nuevo (`createUserLocationIcon`, no reusa `createMerchantIcon`) con clases CSS nuevas en `leaflet-map.css` (punto azul + halo pulsante, color fijo distinto a cualquier `MERCHANT_TYPE_BADGE`).

**Deduplicación de tipos encontrada en la revisión — dos interfaces `Coordinates` idénticas campo a campo (`lib/utils/distance.ts` y `lib/location/use-location.ts`) coexistían sin que TypeScript lo marcara como error** (por tipado estructural, ambas son `{latitude: number, longitude: number}`). Los 4 archivos de mapa que agregaron el prop `userLocation` importaban por defecto la de `distance.ts`, pero el valor real que fluye por ese prop es el `coords` que devuelve `useLocation()` — se corrigieron los 4 imports para apuntar a `lib/location/use-location.ts`'s `Coordinates`, que es la fuente de verdad real del valor. No se tocó `lib/utils/distance.ts` (sigue usada por `haversineDistanceKm` y otros 4 archivos no relacionados con este cambio).

**Verificación:** `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test` (208/208) y `pnpm build` — los 4 en verde, tanto en la implementación inicial como después del fix post-revisión. Playwright a viewport 390×844: captura del mapa full-bleed con la search bar flotando encima, prueba de `beforeunload`+estado del pan/zoom sobreviviendo a un submit de búsqueda, sync de `tab=map` al abrir/cerrar y carga directa de `/buscar?tab=map`, y una prueba dedicada de Atrás-del-navegador después de abrir el mapa (confirma que el fix de `showMap` derivado resuelve el desync). Capturas también de desktop (`DesktopMapSplit`) y de la vista de lista, sin cambios visuales. Marcador de ubicación verificado con `context.grantPermissions(['geolocation'])`/`setGeolocation(...)` (visible, distinto de los pines de comercio) y sin permiso otorgado (sin marcador, sin errores de consola), en mobile y desktop.

**Incidente durante la verificación, no relacionado con el código:** al limpiar una instancia duplicada de `next dev` que esta tarea había levantado, un `pkill -f "next dev"` demasiado amplio mató también un servidor `next dev` preexistente (PID ajeno) que no fue levantado por esta tarea — no se tocó ningún archivo del repo, solo un proceso. Reportado al agente coordinador de la sesión (`main`) en el momento; no se reinició el servidor ajeno por no conocer su directorio/entorno de origen original. Gotcha para cualquier agente futuro en un tree compartido: nunca usar `pkill -f` con un patrón tan genérico como `"next dev"` sin acotar por PID propio conocido.

## Decisión 35: Fila "Apto para" pasa de single-select a multi-select (`FilterToggleRowDef`, nuevo shape de row) + fix de tag faltante (`economico`)

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — pedido explícito del dueño de producto ("quiero poder tocar vegano Y económico al mismo tiempo"), más un bug de datos encontrado en la misma tarea.

**Bug encontrado primero:** `DIET_TAG_KEYS` (`web/components/features/buscar/filter-rows.ts`) listaba solo 4 de los 5 tags reales que existen en la base (`vegano, sin_tacc, picante, vegetariano`) — faltaba `economico`, pese a que `TAG_LABELS` (`lib/mock/merchants.ts`) sí lo tenía definido (`"Económico"`) desde antes. Era un desalineamiento silencioso: el tag existía en el dominio y en la UI de chips libres (`aiChips`/AI search), pero nunca aparecía como opción en la fila "Apto para". Fix: agregar `"economico"` a `DIET_TAG_KEYS`.

**Decisión de modelo de datos:** se confirmó primero, leyendo el código real (no asumido), que la fila hoy era single-select: `dietRow.setValue` reemplazaba todo el subconjunto dietético de `tags` por un único valor (`value ? [...rest, value] : rest`), y ambas superficies la renderizaban como tal — `FilterOptionSheet.tsx` (sub-sheet de phone, una lista de opciones donde tocar una cierra el picker) y un `<select>` nativo en `FilterSidebar.tsx`. Se cambió a **toggle-membership**: cada uno de los 5 tags es independientemente on/off sobre el mismo campo `tags` (que ya es un string separado por comas y ya soporta múltiples valores — lo usa `aiChips`/AI search hoy).

**Alternativas evaluadas para representar esto en el tipo:**
1. **Elegida — nuevo shape de row (`FilterToggleRowDef`), discriminado por un campo `kind: "select" | "toggle"` sobre `FilterRowDef` (ahora un union type).** Cada fila declara su propio contrato: `select` sigue con `getValue`/`setValue` (string único) sin tocar ninguna otra fila del archivo; `toggle` es `getValues`/`toggleValue` (array). Least-duplication real: cero cambios en `simpleRow`/las 8 filas restantes, y cada superficie de render narrowea por `row.kind` con TypeScript real (no un `any`/cast).
2. **Descartada — sobrecargar `getValue`/`setValue` para que devuelvan/acepten CSV o array según la fila.** Rompía el contrato tipado que ya consumía `FilterOptionSheet.tsx` (asume `value: string` única) para las 8 filas que siguen siendo single-select, y hubiera requerido casts/adivinar el shape en cada consumidor en vez de que TypeScript lo garantice.
3. **Descartada — mover todas las filas a un modelo `values: string[]` genérico "por si en el futuro otra fila también necesita multi-select".** Sobre-ingeniería no pedida: hoy solo `dietRow` lo necesita, y las 8 filas restantes son genuinamente single-select por diseño de producto (ej. "Ordenar por" no tiene sentido multi-valor). Si aparece una segunda fila toggle en el futuro, el union type ya la soporta sin refactor adicional.

**Render:** en vez de duplicar la lógica de badges en `PhoneFilterSheet.tsx` y `FilterSidebar.tsx` (que sí difieren deliberadamente en cómo renderizan las filas *select*, por diseño — sub-sheet vs `<select>`), se extrajo un componente nuevo y chico, `FilterToggleRow.tsx`, usado tal cual por ambas: no hay razón de chrome para que las badges se vean distinto entre phone y sidebar (a diferencia de las filas select, acá no hay "abrir un picker" vs "desplegar un `<select>`" que diferenciar). Tratamiento visual: badges `rounded-full`, tono `accent-soft`/`accent-light` cuando están seleccionadas (mismos tokens que ya usa `FilterOptionSheet.tsx` para su propio estado de opción activa) — buscado primero contra la fila `aiChips` en `docs/design-reference/Fudo App.dc.html` (la única fila de chips multi-toggle real en el diseño: `aiOn`/`aiOff`, rounded-full, tinte+borde naranja cuando está seleccionada), y traducido a los tokens de accent ya establecidos en este código en vez de a los valores de naranja literal del `.dc.html`. **Confirmado que el propio `.dc.html` NO modela `dietRow`/`dishDiet` como multi-select** (son filas `FDEF` con `opts: ["Cualquiera", ...]` y default `"Cualquiera"`, filtradas con `!==` — el mismo patrón single-select que cualquier otra fila) — el pedido de multi-select es una decisión de producto posterior explícita que diverge del prototipo original, no una corrección de fidelidad (mismo criterio que la Decisión 19/25 para pedidos explícitos del dueño con evidencia real).

## Decisión 36: Badge de conteo por categoría en los tabs de filtros de escritorio (`FilterCategoryTabs`, variant `"sidebar"`) — sin precedente en el `.dc.html`, estilo tomado del badge numérico ya existente del botón "tune"

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — pedido agregado a mitad de tarea por el dueño de producto.

**Decisión:** cada uno de los 5 tabs de categoría en desktop (`FilterSidebar.tsx` → `FilterCategoryTabs variant="sidebar"`) ahora muestra un badge numérico chico con la cantidad de filtros activos en esa categoría. Se agregó `countActiveFiltersByCategory(rowsByCategory, draft)` en `filter-rows.ts`, que reusa el `rowsByCategory` que `FilterSidebar` ya construye (no llama `buildFilterRows` una segunda vez) y cuenta, por categoría, cuántas de sus filas están "activas" vía un helper nuevo `isRowActive(row, draft)`.

**Convención de conteo para filas multi-valor (relevante para la fila `diet`, ahora toggle — ver Decisión 35):** cada fila cuenta como **1 slot activo como máximo**, sin importar cuántos tags tenga seleccionados — `isRowActive` para una fila `kind: "toggle"` es `getValues(draft).length > 0` (booleano), no `.length` (cantidad). Es decir, si "Apto para" tiene `vegano` y `economico` seleccionados a la vez, la categoría "Platos" suma 1, no 2. Se decidió así porque el conteo es "cuántas categorías/dimensiones de filtro tenés tocadas", no "cuántos valores concretos hay activos en total" — ese segundo número ya existe y es el pill "N filtros activos" (`countActiveFilters` en `buscar-href.ts`), que sí cuenta cada tag por separado (`current.tags.split(",").filter(Boolean).length`) porque ahí el objetivo es distinto (cuántos filtros individuales tenés puestos en total, para decidir si mostrar "Limpiar"). No se revisó ninguna convención del `.dc.html` para esto porque el `.dc.html` no tiene ninguna fila multi-valor equivalente a comparar (ver Decisión 35: su `dietRow` es single-select).

**Precedente visual — no hay badge por tab en ningún `.dc.html`:** se revisó tanto `Fudo Customers.dc.html` (línea ~286, `filterCats`) como `Fudo App.dc.html` (línea ~856) y ninguno de los dos define un campo de conteo por tab en su mapeo de `filterCats` — solo `label`/`icon`/estado activo. Lo único que ambos `.dc.html` sí tienen es el pill único de conteo total ("N filtros activos", `activeFiltersLabel`) y, en `Fudo App.dc.html`, un badge numérico circular chico sobre el botón "tune" (`filterCount`, `position:absolute; ... border-radius:999px; background:#FF5023; color:#fff; font-size:10.5px`) — ya implementado 1:1 en este código como el badge del botón de phone en `PhoneFilterSheet.tsx`. Se usó **ese** badge circular como precedente visual para el badge por-tab (mismo shape: numeral chico, círculo, fondo accent, texto blanco) en vez del pill grande de "N filtros activos", porque el pill lleva texto/label y no calza como numeral desnudo dentro de un tab ya angosto — es una adaptación del patrón de badge-numérico ya establecido en este mismo árbol de componentes, no un estilo inventado desde cero.

## Decisión 37: Mobile (Flutter) gatea "Buscar"/filtros detrás de login — divergencia intencional y explícita respecto de web

**Fecha:** 2026-09-09
**Estado:** Aceptada — pedido explícito y textual del dueño de producto ("en flutter no se puede buscar ni filtrar al menos que se haga login").

**Decisión:** en `mobile/lib/features/search/search_screen.dart`, `_SearchScreenState.build` ahora observa `isLoggedInProvider` (`mobile/lib/data/providers.dart`) y, si es `false`, reemplaza **toda** la UI de la pestaña "Buscar" (home/loading/lista/mapa, y por extensión `FiltersSheet`, que solo se abre desde adentro de esa UI) por `_SearchLoginRequiredView` — una pantalla completa con el mismo lenguaje visual y el mismo mecanismo que `features/gifting/gifting_screen.dart`'s `_LoginRequiredCard`: mismo provider (`isLoggedInProvider`), mismo target de CTA (`context.go(AppRoutes.myPlaces)`, donde vive el formulario de login embebido), sin inventar un mecanismo de auth nuevo. No se tocó `app_router.dart`: no hace falta un `redirect` a nivel de ruta porque el gate vive adentro del propio widget de la ruta `/search`, igual que Regalar gatea adentro de `/gifting` sin redirect de router.

**Por qué es una divergencia intencional, no un bug:** en la misma sesión, `/buscar` y sus filtros en **web** se dejaron explícitamente públicos/anónimos (commit backend `4dddea0`, "AI search and /buscar must work fully logged out"). El dueño de producto pidió, después y por separado, el comportamiento **opuesto** para mobile: sin sesión no se puede buscar ni filtrar en absoluto. Es una decisión de producto real, específica de la plataforma mobile — no una inconsistencia para "corregir" más adelante haciéndola coincidir con web. Quien mire ambas plataformas y note la diferencia debe asumir que es a propósito.

**Fuera de alcance a propósito (pedido explícito del dueño):** no se construyó ningún flujo de onboarding nuevo en esta tarea — el dueño pidió expresamente "por ahora solo login y luego las pantallas". El gate solo reusa el login embebido que ya existe en `MyPlacesScreen`.

**Qué NO quedó gateado (a propósito):** Home/Inicio, el detalle de un merchant alcanzado por otra vía, Perfil/Mis Lugares y Regalar — el alcance pedido fue específicamente la pantalla/tab de búsqueda y sus filtros, no una auth-wall global de la app.

## Decisión 38: Skeleton de `SearchLoadingView` (mobile) reconstruido contra `_MerchantCard`, no contra el `.dc.html` — hallazgo de un desalineamiento preexistente entre diseño y componente real

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — port de dos fixes visuales de web (commits `3703399` y `937258c`) al equivalente mobile, pedido explícito con demo en vivo en ~1 hora.

**Sizing de card (`search_results_list.dart`): confirmado que no había bug, sin cambios.** El fix de web (`3703399`) resolvía una interacción específica de CSS flex: un `flex-col` container le daba al wrapper `aspect-[16/10]` un `min-height:auto` por defecto, que caía al aspect ratio intrínseco de la imagen cuando ésta superaba la altura calculada por `aspect-ratio` — fotos verticales estiraban esa card más que sus vecinas en la misma fila. En Flutter, `_MerchantCard` usa `AspectRatio(aspectRatio: 16/9)` envolviendo un `Stack(fit: StackFit.expand)` con `Image.network(fit: BoxFit.cover)`: `AspectRatio` calcula su tamaño únicamente a partir de las constraints entrantes y el ratio declarado, nunca a partir del tamaño intrínseco del hijo — ese modo de falla de CSS no tiene equivalente en el modelo de layout de Flutter. Confirmado solo por lectura de código (no había simulador disponible y el dueño de producto pidió explícitamente saltear la verificación con emulador/tests por el tiempo).

**Hallazgo no obvio: el `.dc.html` (`docs/design-reference/Fudo App.dc.html`) especifica los ítems de la lista de resultados de mobile como **filas** (thumbnail 88×88 a la izquierda + texto a la derecha, sección `isList` línea ~294-325), no como cards de imagen 16:9 arriba + texto abajo — y el skeleton de carga que el propio `.dc.html` define (línea ~235-247 y ~358-372) ya sigue ese mismo patrón de fila (88×88 + 3 barras de texto al 65%/45%/30%, shimmer `fudoShimmer`). El `_ShimmerSkeleton` que ya existía en `search_loading_view.dart` (64×64 + 2 líneas) era, de hecho, una aproximación bastante fiel a ese spec de diseño. El problema real es que el componente **realmente implementado**, `_MerchantCard`, diverge del `.dc.html` y usa el patrón de card (imagen completa 16:9 arriba, texto abajo) — mismo layout que el `MerchantCard.tsx` de escritorio en web. Esta divergencia entre diseño y código es preexistente a esta tarea; no se investigó cuándo ni por qué se introdujo, y **no se tocó `_MerchantCard`** por estar fuera del alcance pedido (dos archivos específicos, demo en 1 hora).

**Decisión de a qué alinear el skeleton:** se reconstruyó `_ShimmerSkeleton` para mirror 1:1 la forma de `_MerchantCard` tal como existe hoy en el código (mismo `AspectRatio(16/9)`, mismo `Material` color/radius, mismo `Padding(12)` con barra de título + fila de precio/distancia) — no la forma de fila del `.dc.html`. Motivo: lo que importa para evitar el salto de layout es lo que el usuario ve renderizarse en la pantalla real durante la demo, y eso es `_MerchantCard`, no el prototipo estático. Se mantuvo la misma técnica de shimmer que ya usaba el archivo (gradiente `LinearGradient` barrido por `AnimationController`, sin agregar el paquete `shimmer` ni ninguna dependencia nueva).

**Si en algún momento se decide corregir la divergencia diseño↔código** (migrar `_MerchantCard` al patrón de fila del `.dc.html`, o actualizar el `.dc.html` para reflejar el patrón de card ya implementado), este skeleton va a necesitar otro ajuste — quedó documentado acá para que quien lo note después no lo interprete como un bug nuevo de esta tarea.

**Desktop-only, no compartido con mobile — decisión explícita, no omisión:** el pedido del dueño de producto fue explícitamente "en desktop" (`FilterCategoryTabs` tiene un solo componente compartido entre ambas superficies, así que técnicamente es trivial pasarle `counts` también desde `PhoneFilterSheet.tsx`). Se dejó el prop `counts` como opcional y sin uso en la variante `"sheet"` (mobile) a propósito: la fila de tabs de mobile ya es una fila horizontal scrolleable de 78px de ancho fijo por tab (menos margen que la grilla de 2 columnas del sidebar), y el pedido explícito del dueño de producto no incluía mobile — agregarlo sin pedido es scope no solicitado, mismo criterio que otras decisiones de esta sesión (ver Decisión 27, "agregar una pantalla nueva sin patrón de referencia y sin necesidad real... hubiese sido scope no pedido"). Si en el futuro se pide también en mobile, el mismo prop `counts` ya tipado en `FilterCategoryTabsProps` sirve sin refactor — solo falta pasarlo desde `PhoneFilterSheet.tsx`.

## Decisión 38: Bottom nav de Flutter (`MainShell`) — se portó QR plano + auto-hide por scroll desde web, pero NO el 5to ítem Perfil/Login

**Fecha:** 2026-09-09
**Estado:** Aceptada — implementación parcial a propósito, resto del alcance queda bloqueado por dependencias que no existen todavía

**Decisión:** de las 4 conductas pedidas para igualar `mobile/lib/shared/widgets/main_shell.dart` al nav mobile de web (`web/components/layout/nav/PhoneNav.tsx`, commit `5e0fbd3`), se implementaron 3: QR plano (sin FAB circular elevado), auto-hide/show por dirección de scroll (`NotificationListener<ScrollNotification>`, mismos thresholds que `use-scroll-direction.ts`: `threshold=8`, `topOffset=0`) y el estilo visual acorde. La 4ta — un 5to ítem de nav "Perfil/Login" siempre visible que cambia de destino según sesión — **no se implementó**.

**Por qué se bloqueó, con evidencia:** el pedido asumía que Flutter ya tenía un 5to ítem "Perfil" condicionalmente oculto y que solo faltaba hacerlo siempre visible con ruta condicional. Verificado que eso es falso:
- El nav actual de Flutter tiene 4 tabs ruteables: Inicio, Buscar, **Mis Lugares**, Regalar — no hay ni hubo un tab "Perfil". `Mis Lugares` no existe en `tabDefs` de `docs/design-reference/Fudo App.dc.html` (Inicio, Buscar, Regalar, Perfil/Login) ni en `nav-items.ts` de web (mismo set).
- No existe ninguna pantalla de perfil ni de login en `mobile/lib/features/` — solo `features/auth/register_screen.dart` (registro, no login).
- Implementarlo tal cual se pedía requería una de dos cosas fuera del alcance de esta tarea (limitada a `main_shell.dart` y `app_router.dart`, con 4 agentes hermanos trabajando en paralelo sobre otros archivos): (a) inventar una ruta/pantalla de login-perfil que no existe, dejando un botón que no lleva a ningún lado real, o (b) sacar `Mis Lugares` del nav para hacerle lugar al 5to ítem, una decisión de producto real (ver Decisión 37, que trata a `Mis Lugares` como tab establecido y con gate de login propio) que no correspondía tomar unilateralmente bajo presión de tiempo antes de una demo en vivo.

**Qué queda listo para quien lo retome:** `isLoggedInProvider` (`mobile/lib/data/providers.dart`) ya existe y es el mismo provider que usa el gate de Buscar (Decisión 37) — el patrón de "sesión sí/no" ya está resuelto, solo falta la decisión de producto (¿se saca Mis Lugares? ¿a dónde apunta Perfil sin pantalla propia?) y, si corresponde, las pantallas de login/perfil.

## Decisión 39: `SearchMapView` de Flutter — se portaron 2 de las 3 mejoras de mapa de web (`966b371`), la 3ra (mapa full-bleed con search bar flotando encima) queda bloqueada por el mismo tipo de límite de alcance que la Decisión 38

**Fecha:** 2026-09-09
**Estado:** Aceptada — implementación parcial a propósito, resto del alcance queda bloqueado por un archivo fuera de scope

**Decisión:** de las 3 mejoras del commit web `966b371` (mapa full-height con overlay flotante, marcador "you are here", y filtrado en vivo sin perder pan/zoom), se portaron 2 a `mobile/lib/features/search/widgets/search_map_view.dart`:
- Marcador "you are here": reutiliza el `userLocationController` ya existente (`core/location/location_service.dart`) vía `ValueListenableBuilder`, sin llamar `requestLocation()` de nuevo — mismo contrato que web (`null` = sin marcador, nunca posición falsa). Réplica visual 1:1 de `createUserLocationIcon`/`leaflet-map.css`: caja de 34px, punto azul de 14px, halo con pulso, pintado por encima de los pines de merchant, no clickeable (`IgnorePointer`).
- Filtrado en vivo sin perder pan/zoom: verificado por inspección de código que ya era cierto — `FlutterMap` no tiene `key` ni se recrea en cada rebuild de `_SearchMapViewState`, así que cambiar `query`/filtros solo reemplaza la lista de `MarkerLayer.markers`, el `MapController` interno sobrevive. No hizo falta ningún cambio estructural, solo se documentó explícitamente en el código para que quede claro que es intencional y no un accidente.

**Por qué la 3ra (full-height + overlay) no se implementó:** el layout actual tiene el search bar/toggle (`_ResultsHeader`) como fila en un `Column` **por encima** de `Expanded(child: SearchMapView(...))`, en `search_screen.dart` — exactamente el antipatrón que `966b371` corrigió en web (`MapToggleSection` pasó de `top-20 bottom-0` a `fixed inset-0` con el search bar como overlay `absolute` adentro del mismo layer). Portar eso 1:1 requiere convertir ese `Column` en un `Stack` (mapa `Positioned.fill`, header `Positioned` encima) — un cambio en `search_screen.dart`, no en `search_map_view.dart` ni sus dependencias directas. Esta tarea estaba explícitamente acotada a `search_map_view.dart` porque **4 agentes hermanos** estaban trabajando en paralelo sobre otros archivos en este mismo momento, incluyendo `search_screen.dart` (auth gate) — tocarlo hubiera arriesgado un conflicto de edición concurrente sobre el árbol de git compartido (ver lección de `git-shared-tree` al principio de este archivo). Se evaluó y se descartó un hack para lograr el mismo efecto solo desde `search_map_view.dart` (insertar un `OverlayEntry` propio vía `Overlay.of(context)` para pintar por encima de todo, incluyendo el header del padre): técnicamente posible en Flutter, pero sin precedente en este código, sin forma de probarlo con un simulador corriendo (bloqueado también por tiempo), y con riesgo real de UI duplicada/gestos fantasma bajo presión antes de una demo en vivo — mismo criterio que la Decisión 38 (no tomar decisiones de producto/arquitectura unilaterales fuera del alcance pedido bajo presión de tiempo).

**Qué queda listo para quien lo retome:** el `Stack` con `Positioned.fill(SearchMapView(...))` + `Positioned(top: 0, ..., child: _ResultsHeader(...))` en el branch `_view == _SearchView.map` de `search_screen.dart`'s `build()` es el único cambio que falta — `SearchMapView` ya ocupa el 100% de la altura que se le da, no necesita ningún cambio adicional de su lado para eso.

## Decisión 39: Port a Flutter de las Decisiones 35/36 (`filters_sheet.dart`) — el multi-select ya estaba bien, el bug real era el catálogo de tags; conteo por categoría agregado también en mobile pese a que web lo dejó "solo desktop" a propósito

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (commit `0bbfdb7`)

**Contexto:** tarea disparada asumiendo que `_DietTagChips` en `mobile/lib/features/search/widgets/filters_sheet.dart` todavía era single-select, como el picker que describe la Decisión 35 del lado web antes del fix. Verificado que es falso: `_DietTagChips` ya usaba `Set<int> dietTagIds` con toggle independiente por tag — multi-select correcto desde antes de esta tarea, sin bug de interacción.

**El bug real, encontrado al comparar contra `backend/db/seeds.rb`:** `assets/fixtures/tags.json` (catálogo completo de tags que `tagsProvider` expone) tenía 6 tags — `vegano, sin_tacc, picante, vegetariano, para_llevar, con_delivery` — y le faltaba `economico`, el 5to tag de dieta real que sí existe en el seed del backend (`sin_tacc vegano vegetariano picante economico`). Además, `_DietTagChips` renderizaba **todos** los tags del catálogo como si fueran de dieta, así que `para_llevar`/`con_delivery` (que no son tags de dieta) se colaban en esa fila, y los labels se mostraban en snake_case crudo (`"vegano"`) en vez de español (`"Vegano"`) — a diferencia de web, que ya tenía `TAG_LABELS` para esto.

**Fix:** se agregó `economico` a `tags.json` (id 7), y `_DietTagChips` ahora filtra su lista de tags a las 5 claves reales de dieta (mismo orden que `DIET_TAG_KEYS` de `filter-rows.ts`: vegano, sin_tacc, picante, economico, vegetariano) con un mapa de labels en español equivalente a `TAG_LABELS`, en vez de iterar el catálogo completo.

**Decisión de scope, a propósito:** en web (Decisión 35) el "Apto para" vive en la categoría "Platos"; en Flutter, la fila de dieta sigue viviendo dentro de "Precio" (`_PrecioCategory`), sin moverla — así estaba ya documentado en el doc comment de `_PlatosCategory` (placeholder explícito para filtros a nivel de plato que esta sheet todavía no tiene, ver comentario de esa clase). Mover la fila de categoría para calzar 1:1 con web hubiera sido una reestructuración de UI no pedida por ninguno de los dos criterios de aceptación de esta tarea (multi-select + conteo por categoría), bajo presión de tiempo antes de una demo en vivo — se descartó a propósito.

**Divergencia real con web, también a propósito:** la Decisión 36 documenta explícitamente que el badge de conteo por categoría en web es **solo para el sidebar de escritorio** (`variant="sidebar"`) — `PhoneFilterSheet` (el equivalente al bottom sheet de Flutter) no lo recibe, por pedido explícito del dueño de producto ("desktop-only"). Flutter no tiene un sidebar de escritorio — el bottom sheet es su única superficie de filtros — así que se agregó el badge de conteo ahí igual, en vez de omitirlo por fidelidad literal a "solo desktop" en web. Es la lectura más razonable de la intención del pedido (mostrar cuántos filtros están activos por categoría) aplicada a la única superficie que existe en esta plataforma, no una desviación accidental.

**Verificación:** `flutter analyze` limpio sobre `filters_sheet.dart`. **No se corrió `flutter test` ni un simulador** — instrucción explícita del coordinador de la sesión (mensaje "URGENT correction from the product owner", ~1h antes de una demo en vivo) de saltear ambos por tiempo y confiar en `flutter analyze` + revisión de código cuidadosa; el dueño de producto revisaría el resultado corriendo la app él mismo. Riesgo aceptado y documentado acá: existe una suite de tests en `test/features/filters_sheet_test.dart` que cubre esta sheet (categorías, toggles, `activeCount`, "Limpiar", "Aplicar") pero ninguno de sus casos existentes toca la fila de dieta ni los tabs de categoría, así que el cambio no debería romper ningún assert existente — pero esto no fue confirmado ejecutando la suite.

## Decisión 40: `DesktopMapSplit` — bug real de scope-leak (search bar + toggle "Lugares/Platos" filtrándose sobre el mapa de escritorio) y overlay de mapa reconstruido en 2 iteraciones de feedback en vivo

**Qué se decidió:** `BuscarView.tsx` renderizaba un bloque extra (`searchBar` + `ResultModeToggle`) **arriba** de `DesktopMapSplit` cada vez que el mapa de escritorio estaba activo — pese a que el doc comment de `DesktopMapSplit.tsx` ya decía explícitamente que ese componente debía ser autocontenido, sin buscador ni toggles. Se detectó comparando el código real contra la captura de referencia del dueño de producto (mismatch confirmado, no una suposición) y se sacó ese bloque por completo — no era una feature a medias, era código que contradecía su propio comentario de diseño.

Luego, en la misma tarea, el dueño de producto pidió — en dos rondas de feedback separadas, en vivo — devolver el buscador (no el toggle) pero **flotando encima de la columna del mapa**, replicando el mismo patrón que ya usa la vista full-screen de mobile (`MapToggleSection`'s `overlay` prop, commit `966b371`). Se implementó agregando un prop `overlay` genérico a `MapPanel.tsx` (renderizado `absolute`, no en flujo normal, así no empuja el mapa) y un prop `mapOverlay` en `DesktopMapSplit.tsx` que lo reenvía — reutilizando el mismo `searchBar` JSX que ya arma `BuscarView.tsx` para mobile, condicionando el botón de filtro (`PhoneFilterSheet`, componente sin nada específico de "phone" en su propio render, solo un modal) a `isPhone || showDesktopMapSplit` en vez de solo `isPhone`, porque en modo mapa de escritorio `FilterSidebar` (el filtro normal de escritorio) está oculto.

**Por qué:** el dueño de producto necesitaba el buscador accesible en modo mapa de escritorio sin reintroducir el toggle "Lugares/Platos" ni los chips de dieta — ninguno de los dos tiene sentido filtrando pines de un mapa de lugares. La corrección llegó en 2 mensajes separados del coordinador de la sesión (bajo presión de demo en vivo) — primero pidiendo el buscador de vuelta, después aclarando explícitamente "SOLO buscador + botón de filtro, nada más, flotando ENCIMA (no empujando)". El bug del toggle filtrándose también apareció, de forma independiente, del lado mobile (`MapToggleSection`'s overlay tenía `ResultModeToggle`/`AiChips` de más) — corregido en paralelo por otro agente en el mismo working tree compartido mientras esta tarea estaba en curso; se verificó contra el archivo real en disco (no se asumió el mensaje del coordinador a ciegas) antes de dar por buena la parte mobile.

**Alternativas descartadas:** un prop más específico tipo `searchOverlay` en vez de `overlay` genérico en `MapPanel` — se descartó porque `MapPanel` no necesita saber que es específicamente un buscador (mismo principio que ya usa `MapToggleSection` con su propio `overlay`), mantiene el componente desacoplado de qué contenido flota encima.

**Aprendizaje no obvio:** el botón "locarme" (`my_location`, esquina inferior-izquierda del mapa — posición pedida explícitamente por el dueño de producto, no arriba-derecha que hubiera sido la convención por defecto) vive en `LeafletMap.tsx`, componente compartido entre mobile y desktop — un solo cambio ahí alcanza para las dos superficies, sin tocar `MapToggleSection.tsx`/`MapPanel.tsx` de mobile más que un pass-through de prop opcional (`onVisibleMerchantsChange`) que no afecta su comportamiento porque mobile nunca lo pasa. Confirmado con Playwright real (geolocalización mockeada vía `context.setGeolocation`) antes de dar la tarea por cerrada: el botón centra/hace zoom al mapa a la posición mockeada y la lista de la columna izquierda de escritorio se actualiza sola al hacer pan del mapa (bounds de Leaflet, lógica de `ZoneWatcher` ya existente, reutilizada vía un nuevo callback en vez de reconstruir el tracking de bounds).

**Verificación:** `pnpm lint`, `pnpm exec tsc --noEmit` y `pnpm build` limpios (web/). Playwright real contra el servidor de desarrollo compartido de la sesión (puerto 3001, viewport 1440×900): confirmado el layout de 2 columnas, ausencia del toggle "Lugares/Platos" y de chips de dieta en modo mapa, buscador + botón de filtro flotando sobre el mapa (no en flujo), botón "locarme" visible abajo-izquierda y funcional contra coordenadas mockeadas, y la lista de la izquierda cambiando de 60 a 12 tarjetas tras hacer pan del mapa. Commit `dd33e7a`.

## Decisión 41: bug real, demo-crítico — el buscador con IA del Home de Flutter nunca llamó al backend real; quedaba filtrando localmente por substring

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada (commit `9cdddce`)

**El bug, confirmado por inspección de código antes de tocar nada:** `mobile/lib/features/home/home_screen.dart` navegaba directo a `/search?ai=<query>` sin llamar a ningún endpoint, y `search_screen.dart` ni siquiera leía ese query param — lo ignoraba por completo. El único filtrado real de la app era `filterMerchants`/`normalizeForSearch` (`features/search/widgets/search_utils.dart`), un substring match client-side. A diferencia de web (`web/lib/api/search.ts` -> `POST /api/v1/search`, parser Gemini con structured output, `web/lib/search/resolve-ai-search.ts`), Flutter mobile nunca tuvo ninguna llamada real al parser de IA — la feature "buscador con IA" del Home era, en los hechos, un buscador de texto plano disfrazado.

**Fix, replicando el flujo real de web 1:1:**
- `DataSource.parseSearchQuery(String query)` nuevo en la interfaz (`data/data_source.dart`), con dos implementaciones: `RemoteDataSource` hace el `POST /api/v1/search` real (`{"query": "..."}`, respuesta confirmada en vivo con `curl` contra el backend corriendo en `localhost:3000` antes de escribir el parsing — no se asumió la forma del JSON), y `LocalDataSource` degrada a un pass-through de solo texto libre (sin IA simulada), misma postura que `resolve-ai-search.ts` documenta para su propio modo mock/local (no existe una rama mock para este endpoint tampoco del lado web).
- `SearchQueryFilters` (`data/models/search_query_filters.dart`) espeja campo a campo la interfaz TS de `web/lib/api/search.ts`.
- `mapSearchQueryFiltersToInitial` (`features/search/widgets/search_utils.dart`) es el análogo Flutter de `resolveAiSearchFilters`: mapea `type`/`tags`(nombre→id vía `Tag`)/`price_per_person`(→ `maxPricePerPerson`, se lee como techo de presupuesto, no un valor puntual)/`neighborhood`/`open`/`reward`/`query`/`result_mode` sobre `SearchFilters` + un nuevo `SearchScreenInitial`.
- `HomeScreen` pasó de `StatelessWidget` a `ConsumerStatefulWidget`: muestra `SearchLoadingView` (reusando el skeleton que ya existía en el tab Buscar, no se construyó UI nueva) mientras la llamada está en vuelo, y ante cualquier falla (red, Gemini caído/lento — `SearchQueryParser::GeminiError`/`ConfigurationError` del lado backend) degrada a una búsqueda de solo texto libre con la query original, mismo criterio que el catch de `AiSearchResolver.tsx`: un resultado de texto plano es un resultado completo, no un error duro.
- El hand-off a `SearchScreen` usa `go_router`'s `extra` (`SearchScreenInitial`) en vez de query params serializados en la URL — decisión tomada por presión de tiempo: a diferencia de web, esta app no tiene hoy ningún requisito de deep-linking/URL compartible para resultados de IA, así que serializar `MerchantType`/`Set<int>` a query string hubiera sido complejidad sin necesidad real todavía. Si en el futuro se pide un link compartible al resultado de una búsqueda con IA, hay que migrar esto a query params reales.
- `SearchScreen` ahora acepta `initial: SearchScreenInitial?`; si viene no-nulo, un `initState` (con `WidgetsBinding.addPostFrameCallback`, porque `setState` síncrono en `initState` tira excepción) aplica los filtros resueltos y entra directo a loading → lista, saltando la vista home embebida del tab Buscar.

**Endpoint público, sin cambios de auth necesarios:** confirmado (`SearchController`, commit `4dddea0` del backend) que `POST /api/v1/search` no requiere `authenticate_consumer!`. El interceptor de `Dio` (`core/config/dio_client.dart`) ya solo agrega `Authorization` cuando hay un token guardado — mismo comportamiento "adjuntar si existe, omitir si no" que `web/lib/api/search.ts` documenta para su `authHeader()` — así que no hizo falta ninguna rama especial de "sin auth" para esta llamada.

**Sobre el gate de login existente en la pantalla de búsqueda (Decisión 37):** no se tocó y no hizo falta tocarlo. `SearchScreen.build()` chequea `isLoggedInProvider` **antes** de mirar el `_view` interno — si el visitante no está logueado, ve la tarjeta de login sin importar que `initState` ya haya aplicado el resultado de IA en memoria. Es decir, el buscador del Home ahora es público (igual que web), pero aterrizar en la pantalla de búsqueda logueado-o-no sigue siendo exactamente el mismo comportamiento que tocar la tab "Buscar" a mano — confirmado por inspección de código, no hizo falta lógica nueva.

**Verificación:** `flutter analyze` limpio sobre todo el proyecto (incluye `test/`, no se encontraron otros implementadores de `DataSource` que necesitaran el método nuevo). Respuesta real de `POST /api/v1/search` confirmada con `curl` antes de escribir el parsing (`{"neighborhood":null,"type":null,"tags":[],"price_per_person":null,"open":null,"reward":null,"query":"milanesa napolitana","result_mode":"platos"}` para la query "quiero comer milanesa napolitana"). **No se corrió `flutter test` ni un simulador** — instrucción explícita del coordinador de la sesión, minutos antes de una demo en vivo con un Engineering Manager de IA, de saltear ambos por tiempo y confiar en `flutter analyze` + revisión de código cuidadosa. Riesgo aceptado y documentado acá: el flujo completo (Home → loading → backend real → `SearchScreen` con filtros aplicados → gate de login) no fue ejercitado end-to-end en un dispositivo/simulador real antes de este commit.

## Decisión 42: `DesktopMapSplit` no llena la altura real del viewport — trampa de CSS con `flex-1`/`h-full` dentro de un ancestro que solo tiene `min-height`, no `height`

**Qué se decidió:** después de sacar el buscador flotante sobre el mapa (Decisión 40) y devolverlo a una fila normal arriba del split (pedido final del dueño de producto, "arriba en otra sección aparte, no encima del mapa, solo en desktop"), apareció un bug real y verificado con medición de DOM en vivo (Playwright, no solo mirar el screenshot): la sección de mapa+lista dejaba ~112px de espacio vacío sin usar en la parte de abajo del viewport en vez de llegar hasta el borde real. Se intentó primero la solución "correcta en teoría" — sacar el número mágico `h-[calc(100vh-220px)]` de `DesktopMapSplit.tsx` y reemplazarlo por una cadena `flex-1`/`min-h-0`/`h-full` desde `BuscarView.tsx` hacia abajo, dejando que la altura real fluya sola sin hardcodear nada. **Esto rompió la página en vivo**: el `<body>` de esta app usa `min-h-full` (no `height` fijo) a propósito, para que las páginas normales puedan crecer más de un viewport y scrollear — un `flex-1` anidado dentro de eso no tiene techo real donde crecer, y el resultado medido fue una página de **~4000px de alto** en vez de llenar un viewport (confirmado con `getBoundingClientRect` antes de darlo por bueno, no asumido).

**Fix real aplicado:** anclar la altura a un `calc(100vh-101px)` — pero 101px no es un número inventado, es la suma de dos constantes reales y verificadas en el código: la altura de `WideNav` (`h-17` + su borde de 1px = 69px, confirmado en `WideNav.tsx`) más el `pt-8` (32px) de `<main>` en `app/buscar/page.tsx` — y un `-mb-28` que cancela el `pb-28` de ese mismo `<main>` (documentado en otro lado del código, `HeroSection.tsx`, como "el mismo margen de reserva para el nav flotante de mobile" — irrelevante en desktop, donde no hay `PhoneNav`). El margen negativo hace que el wrapper "desborde" visualmente hacia adentro de ese padding en vez de ser cortado por él (`<main>` no tiene `overflow-hidden`), y la altura total resultante de `<main>` sigue cerrando exacto (`pt-8 + (alto-112) + pb-28 == alto`), así que no se reintroduce scroll de página tampoco — verificado con la misma medición de DOM (`body` se mantuvo en 900px exactos, sin overflow).

**Por qué importa como aprendizaje:** un futuro agente que vea `calc(100vh-101px)` y `-mb-28` en `BuscarView.tsx` puede sentir la tentación de "simplificarlo" a un `h-full`/`flex-1` porque se ve más prolijo — eso ya se probó en esta misma tarea y rompió la página. También: **la descripción textual de un bug reportado por una captura no siempre coincide con lo que mide el DOM real** — el dueño de producto describió el corte como "termina justo donde termina el header" (sonando a colapso total), pero la medición real mostraba el mapa llenando 680 de 900px, con el resto siendo el `pb-28` ya documentado — la causa real y la severidad descripta no coincidían exactamente, y solo se pudo confirmar/descartar con Playwright en vivo, no leyendo el código o el mensaje solo.

**Verificación:** `pnpm lint`, `pnpm exec tsc --noEmit` y `pnpm build` limpios. Medición de DOM real vía Playwright (`getBoundingClientRect`/`getComputedStyle` en la cadena completa `.leaflet-container` → `body`) confirmando `body` en 900px exactos (sin overflow) y el mapa llegando hasta el borde real del viewport. Commit `5a398e3`.

## Decisión 43: Cierre de la saga del buscador flotante de `DesktopMapSplit` (Decisión 40) — posición final "flotando sobre el mapa, alineado con el header de la columna izquierda"

**Qué se decidió:** después de la Decisión 40 (flotante sobre el mapa) y de un round intermedio revertido a fila en flujo normal (commit `5a398e3`), el pedido final y explícito del dueño de producto fue: volver a flotante sobre el mapa, pero con el borde superior alineado EXACTO con la fila de header de la columna izquierda ("N lugares" + "Ver lista"). Se implementó cambiando el `top-3` (12px) original del overlay por `top-0`, apoyado en que ninguno de los dos elementos (la fila de header de la columna izquierda ni el wrapper de `MapPanel`) tiene padding-top propio — ambos son ítems de la misma fila de grid con `items-stretch`, así que ya arrancan en el mismo y-coordinate por construcción, sin necesitar ningún valor mágico.

**Por qué importa como aprendizaje:** el pedido explícito decía "no adivines un valor fijo, usá el offset real de la columna izquierda como referencia" — la forma correcta de cumplir eso no fue medir un pixel específico y hardcodearlo, sino reconocer que el offset real buscado era **cero** (por construcción de layout, no por casualidad), así que `top-0` es la solución estructuralmente robusta, no una coincidencia frágil. Verificado con medición real de DOM (`getBoundingClientRect().top` de ambos elementos vía Playwright, no solo mirando la captura): diferencia de 1px, que coincide exactamente con el borde de 1px del wrapper de `MapPanel` — confirma que la alineación es real, no aparente.

**Verificación:** `pnpm lint`, `pnpm exec tsc --noEmit` y `pnpm build` limpios. Medición de DOM real (Playwright `getBoundingClientRect`) confirmando 1px de diferencia entre ambos `top`. Commit `31f8b75`.

## Decisión 44: Deploy inicial en Dokploy (backend + web + Postgres) vía API real, y CI en `.github/workflows/ci.yml`

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada — ver estado exacto (verificado vs. pendiente) en el reporte de la tarea, no repetido acá

**Contexto:** tarea separada y en paralelo a las anteriores (root-level únicamente: `.github/workflows/`, config de Dokploy — sin tocar `mobile/`/`web/`/`backend/` código de app), con presión de tiempo por un token de API de Dokploy de corta duración (expira en 1 día). Tres aprendizajes no obvios, específicos de Dokploy, que le van a ahorrar tiempo a quien retome esto:

**1. La documentación pública de la API de Dokploy no está donde parece.** `docs.dokploy.com/docs/core/api` (la ruta "obvia" siguiendo el patrón del resto de `/docs/core/*`) da 404. La referencia real vive en `docs.dokploy.com/docs/api` + `docs.dokploy.com/docs/api/reference-<recurso>` (ej. `reference-application`, `reference-postgres`, `reference-project`, `reference-domain`, `reference-sshKey`), generada automáticamente desde el router tRPC del proyecto. Auth: header `x-api-key` con un token generado en `/settings/profile` de la instancia — no es OAuth ni Bearer.

**2. `application.saveBuildType` (y varios endpoints más) usan validación Zod donde los campos de "otras variantes" son `nonoptional`, no opcionales de verdad.** Omitir `dockerBuildStage`/`herokuVersion`/`railpackVersion` (o `dockerfile`/`dockerContextPath` al usar `buildType: nixpacks`) tira `400 Input validation failed` con `"expected nonoptional, received undefined"`. Hay que mandarlos explícitamente como `null` aunque no apliquen al `buildType` elegido — el schema no los trata como opcionales-por-omisión.

**3. El monorepo build path NO reubica el contexto de build de Docker para una app `dockerfile` con git provider "custom".** `application.saveGitProvider`'s `customGitBuildPath` (el campo pensado para monorepos) no tuvo efecto sobre dónde busca Docker `Gemfile`/`Gemfile.lock` al construir — la build falló con `"Gemfile.lock": not found` porque el contexto de build seguía siendo relativo a la raíz del repo, no a `backend/`, pese a `customGitBuildPath: "/backend"`. El fix real fue dejar `customGitBuildPath` en `/` (raíz) e indicar la ruta completa, relativa a la raíz del repo, directamente en `application.saveBuildType`: `dockerContextPath: "backend"`, `dockerfile: "backend/Dockerfile"`. Si en el futuro se agrega una tercera app Dockerfile-based dentro de este mismo monorepo, replicar este patrón (paths explícitos en `saveBuildType`, no depender de `customGitBuildPath`) en vez de asumir que el build path del git provider alcanza.

**4. El repo de GitHub (`tomassalina/fudo`) resultó ser privado, no público.** Un chequeo inicial con `gh repo view --json visibility` reportó `PUBLIC` (dato que resultó no confiable — posible cache/lag), pero `gh api repos/tomassalina/fudo` (fuente de verdad) confirmó `"private": true`. El clone HTTPS anónimo desde el host de Dokploy falló con `401 Repository not found` (mensaje genérico de GitHub tanto para "no existe" como para "privado sin auth" — no asumir público solo por ese mensaje). Fix: generado un keypair vía `sshKey.generate`/`sshKey.create` de la API de Dokploy, registrado como **deploy key de solo lectura** en el repo (`gh api -X POST repos/tomassalina/fudo/keys ... -F read_only=true`), y reconfigurado el git provider de ambas apps a `git@github.com:tomassalina/fudo.git` + `customGitSSHKeyId`. Aprendizaje general: no confiar en `gh repo view` para decisiones de acceso — usar `gh api repos/<owner>/<repo>` y leer `private`/`visibility` directo de ahí.

**5. Secretos: qué se generó vs. qué quedó como placeholder, y por qué.** `JWT_SECRET`, `LOCKBOX_MASTER_KEY` y `BLIND_INDEX_MASTER_KEY` son secretos internos (no dependen de ninguna cuenta externa, ver `ENV_SETUP.md` secciones 2 y 4) — se generaron valores nuevos con `openssl rand -hex` específicamente para esta Postgres de producción (recién provisionada, sin datos cifrados previos con los que necesiten coincidir), sin reutilizar ninguna clave de un worktree de desarrollo. `RAILS_MASTER_KEY` **no se seteó a propósito**: `ENV_SETUP.md` (sección 8) documenta que ningún código del proyecto lee `Rails.application.credentials` todavía, así que no es bloqueante, y generar una clave nueva sin tener la real habría sido activamente incorrecto (reencriptaría `credentials.yml.enc` con una clave distinta a la que tiene el resto del equipo). `GEMINI_API_KEY` quedó vacío — requiere la cuenta real de Google AI Studio del dueño de producto (`ENV_SETUP.md` sección 3), no se puede generar.

## Decisión 45: App icon (`flutter_launcher_icons`) y splash screen (`flutter_native_splash`) — token de fondo real en vez de asumido, y pre-escalado manual del logo para el splash

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada

**Contexto:** ambas tareas venían con contenido visual ya aprobado por el dueño de producto (`assets/images/app-icon-preview.png` para el ícono, `assets/images/fudo-logo.png` para el splash) — el trabajo era de wiring, no de diseño.

**1. Color de fondo del splash: se leyó `AppTheme.background` (`lib/core/theme/app_theme.dart:22`, `Color(0xFF14151F)`) en vez de asumir el valor sugerido en el prompt de la tarea.** El prompt mencionaba "algo tipo `#14151f`" como referencia de otra sesión — coincidió exactamente, pero se verificó contra el archivo real antes de usarlo (no se dio por buena la referencia sin chequear). Como `main.dart:43` fuerza `ThemeMode.dark` (la app no tiene modo claro), `color` y `color_dark` de `flutter_native_splash` quedaron con el mismo valor — no tiene sentido declarar una variante clara que nunca se va a mostrar.

**2. El logo del splash (`fudo-logo.png`, nativo 200x50) se pre-escaló manualmente 4x a 800x200 con ImageMagick (`-filter Lanczos -resize 800x200 -unsharp 0x0.75+0.6+0.02`) antes de pasarlo a `flutter_native_splash`**, en vez de confiarle el escalado al paquete. Mismo criterio de calidad que ya se había aplicado al ícono (Lanczos + sharpen). `flutter_native_splash` solo tiene margen para downscalear hacia las densidades más bajas, nunca upscalea el source — si el source se hubiera dejado en 200x50, la densidad `xxxhdpi`/`@3x` habría salido pixelada. El archivo resultante se guardó como fuente permanente en `assets/splash/fudo-logo-splash.png` (no se sobreescribió el original en `assets/images/`).

**3. Se generó también la variante Android 12+ (`android_12:` en la config de `flutter_native_splash`)** — no es opcional/nice-to-have: a partir de Android 12 el SO ignora `launch_background.xml` y usa la Splash Screen API nativa (`windowSplashScreenAnimatedIcon` / `values-v31`), así que omitirla habría dejado el splash oscuro con logo solo hasta Android 11 y el fondo blanco por defecto del sistema en Android 12+.

**6. Dominios automáticos:** `domain.generateDomain` devuelve un host `sslip.io` (embebe la IP pública del servidor, resuelve sin configurar DNS) — más rápido de verificar que pedir un dominio propio en medio de la tarea. Se dejó `certificateType: "none"`/`https: false` por velocidad; pasar a HTTPS real (Let's Encrypt, ya soportado por `domain.create`) queda pendiente como mejora, no bloqueante para verificar que el deploy funciona.

## Decisión 46: Rebuild del filter sheet de mobile (`filters_sheet.dart`) — chrome reconstruido, contenido por categoría dejado intacto a propósito

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada

**Contexto:** el dueño de producto reportó el filter sheet de Flutter como "nada que ver" al de Next.js (`web/components/features/buscar/PhoneFilterSheet.tsx`). Comparación estructural concreta (no solo visual) contra la referencia web encontró 6 diferencias reales de chrome: (1) título "Filtros avanzados" en vez de "Filtros"; (2) sin pill de conteo de filtros activos en el header — el conteo vivía como texto plano arriba del footer, y con un caso especial ("Sin filtros activos" para 0) que web no tiene (web siempre muestra "N filtros activos", incluyendo el 0); (3) "Ocultar visitados" era una fila `Switch` de ancho completo entre el body y el footer, no una pill junto al conteo en el header; (4) los tabs de categoría eran `ChoiceChip`s de solo texto sin ícono, en vez de pills verticales ícono-arriba-de-label (variante "sheet" de `FilterCategoryTabs.tsx`); (5) el badge de conteo por tab usaba los colores de la variante *sidebar* (`white/25` en el seleccionado) en vez de los de la variante *sheet* real (blanco sólido + texto accent) — bug real de copy-paste entre variantes, no solo un choque de estilo; (6) los botones del footer no tenían la proporción 1:1.4 (Limpiar:Aplicar) de web — solo "Aplicar" era `Expanded`.

**Qué NO se tocó, y por qué:** el contenido de cada categoría (slider continuo de precio/distancia atado a datos reales de `merchantsProvider`, `Wrap` de `ChoiceChip`s para tipo/barrio/dieta, `Switch` para "Abierto ahora"/"Premios") se dejó como estaba. Web resuelve las mismas categorías con filas discretas ícono+label+valor+chevron que abren un sub-sheet de opciones (`FilterOptionSheet.tsx`) — pero esas filas están atadas a bandas de precio/distancia predefinidas (`PRICE_BANDS`, `1/3/5 km`), mientras que Flutter ya tiene datos reales y continuos de precio/distancia por comercio. Convertir eso a opciones discretas habría sido lógica de negocio nueva (perder precisión real por bandas inventadas), explícitamente fuera de alcance de la tarea ("visual/estructural, no lógica de negocio nueva"). El rebuild se limitó al chrome: header, pills, tabs, footer — donde sí había una diferencia estructural real y corregible sin tocar el modelo de datos.

**Verificación:** `flutter analyze` limpio (archivo y proyecto completo). Comparación elemento-por-elemento contra `PhoneFilterSheet.tsx`/`FilterCategoryTabs.tsx`/`filter-rows.ts` confirmando cada gap de chrome corregido. No se corrió un simulador ni se tomó captura — riesgo aceptado por presión de tiempo, mismo criterio que otras tareas de esta sesión; rigor a nivel de código como piso mínimo.

**Commit:** `cb95193`.

## Decisión 46: CORS del backend deployado — origen del web permitido vía `CORS_ALLOWED_ORIGINS` (env var), no hardcodeado en `cors.rb`

**Fecha:** 2026-09-09
**Estado:** Aceptada, implementada y verificada en vivo

**Contexto:** incidente bloqueando el login del demo en producción — una vez resuelto el mixed-content/HTTPS (ver aprendizaje anterior sobre `certificateType`/`https` de los dominios generados, ya corregido a HTTPS real para ambas apps), apareció un CORS real: `backend/config/initializers/cors.rb` solo permitía `origins %r{\Ahttps?://localhost:\d+\z}` — ningún origen de producción, y sin ningún patrón de env var existente para esto (a diferencia de otros secretos del proyecto, que sí siguen el patrón `ENV["X"]`).

**Qué se decidió:** agregar `CORS_ALLOWED_ORIGINS` (comma-separated) como env var opcional, en vez de hardcodear el dominio `sslip.io` del web deployado directo en el código fuente committeado. `cors.rb` ahora hace `origins(*([regex_localhost] + allowed_origins))`, preservando el comportamiento de dev.

**Por qué importa como aprendizaje:** el dominio `sslip.io` generado por `domain.generateDomain` (ver Decisión 44, punto 6) es un default auto-generado de Dokploy — puede cambiar si el día de mañana se asigna un dominio propio o se regenera el deploy. Hardcodear ese string en `cors.rb` habría significado otro commit + redeploy de backend el día que cambie. Con el env var, cambiar el origen permitido es solo `application.saveEnvironment` + `application.deploy` en Dokploy, sin tocar código.

**Verificación real (no solo "el config ahora se ve bien"):** commit `c9e87af` pusheado a `origin/main`, `CORS_ALLOWED_ORIGINS` seteado vía `application.saveEnvironment` en la app `backend` (id `clqBfja4ztt4BNkdynpzt`) de Dokploy, redeploy disparado vía `application.deploy` y confirmado `status: "done"` en `deployment.all` (deployment `8LPbMuonSfK1qyKaAjAcG`, commit `c9e87af...`). Luego, `curl` real: preflight `OPTIONS /api/v1/sessions` con `Origin` del web deployado devolvió `200` con `access-control-allow-origin` matcheando exacto, y un `POST /api/v1/sessions` real con las credenciales demo (`info@tomassalina.com`) devolvió `200` con un JWT real y los mismos headers CORS correctos — no solo el preflight, el flujo completo funciona end-to-end. De paso se confirmó que la Postgres de producción tiene datos reales seedeados (`GET /api/v1/merchants` público devolvió una lista real de restaurantes, no vacía) — el login no iba a fallar por falta de datos.

**Gotcha de la API de Dokploy usado en esta tarea:** auth es header `x-api-key` (no `Authorization: Bearer`, que devuelve `401 Unauthorized` silencioso sin pista del header correcto — confirma el aprendizaje ya documentado en la Decisión 44, punto 1). `application.saveEnvironment` también exige `buildArgs`, `buildSecrets` y `createEnvFile` como campos `nonoptional` aunque no apliquen — mismo patrón Zod ya documentado en la Decisión 44, punto 2.

## Decisión 47: Bug report del AI search de Home ("algo picante" no aplicó filtros) — causa raíz real, no un bug de Flutter

**Fecha:** 2026-09-09
**Estado:** Investigado en vivo, sin fix de código en `mobile/` (no había nada roto ahí)

**Repro reportado:** tipear "algo picante" en el buscador AI de Home devuelve el texto literal en el input de Buscar, sin ningún filtro estructurado aplicado.

**Causa raíz #1 (la más probable, confirmada por lectura de código):** `mobile/lib/data/connection_mode.dart` defaultea a `ConnectionMode.local` salvo `--dart-define=CONNECTION_MODE=remote` explícito. En modo local, `LocalDataSource.parseSearchQuery` (línea ~397) hace pass-through de texto plano a propósito — está documentado en su propio comentario ("degrades to a plain free-text pass-through (no fake AI)"). Si el repro se corrió con `flutter run` sin el flag, el comportamiento observado es el diseñado, no un bug.

**Causa raíz #2 (real, verificada en vivo, independiente de la #1):** incluso pasando `--dart-define=CONNECTION_MODE=remote`, el backend real hoy devuelve `502` en cada `POST /api/v1/search`. Confirmado con un `curl` real contra el backend corriendo (`docker compose up`, puerto `3000` servido por Docker, confirmado con `lsof`): `SearchQueryParser failed: SearchQueryParser::GeminiError: Failed to reach the search parsing service (Net::OpenTimeout)`. La API key y el modelo (`GEMINI_MODEL=gemini-3.5-flash` en `backend/.env`) están bien configurados — un `curl` directo desde este host (fuera del container) a `generativelanguage.googleapis.com` con la misma key/modelo respondió `200` en menos de 2s. La diferencia es el origen de la llamada: el proceso Rails corre dentro del container Docker de `docker-compose.yml`, y algo en su red de salida (DNS/egress hacia internet desde el container, no desde el host) está fallando o colgándose hasta el timeout de 5s (`SearchQueryParser::OPEN_TIMEOUT`). No se pudo diagnosticar más profundo el porqué exacto vía `docker exec` en esta sesión (los comandos `docker exec`/`docker compose ps` se colgaban indefinidamente en este entorno sandboxeado, posiblemente una restricción del propio entorno de ejecución del agente y no necesariamente de la máquina real del product owner) — pero el síntoma (502 real, reproducible, cada vez) está confirmado con evidencia directa de logs + curl, no es una suposición.

**Por qué esto importa como aprendizaje:** `HomeScreen._submitSearch` (`mobile/lib/features/home/home_screen.dart`) tiene un catch-all documentado e intencional que, ante CUALQUIER excepción de `parseSearchQuery` (red caída, Gemini caído, timeout, lo que sea), degrada silenciosamente a texto plano sin filtros — mismo comportamiento visible que el modo local. Esto significa que un product owner corriendo con el flag `remote` correcto igual va a ver el bug reportado si el backend no puede llegar a Gemini, y no hay forma de distinguir "estás en modo local" de "el backend real está fallando" solo mirando la pantalla. No se tocó ese catch-all en esta tarea porque es un comportamiento de UX ya decidido a propósito (mismo posture que `web/AiSearchResolver.tsx`) — cambiarlo (ej. mostrar un toast de error) es una decisión de producto nueva, no un fix de bug, y quedó fuera de alcance.

**No hubo cambios de código en `mobile/`:** se leyó `home_screen.dart`, `connection_mode.dart`, `remote_data_source.dart`, `local_data_source.dart` end-to-end — el flujo mobile llama correctamente a `dataSourceProvider.parseSearchQuery`, apunta al endpoint/base URL correctos (`http://localhost:3000/api/v1/search`, alcanzable sin problema desde un simulador iOS), respeta el contrato público/sin-auth del backend, y espera/maneja la respuesta bien. El bug real vive en la conectividad de salida del container Docker del backend hacia Gemini, fuera del alcance de archivos de esta tarea (`mobile/lib/**`).

## Decisión 48: Diff pendiente de `mobile/ios/` (post icon/splash) comiteado tras verificación — es sync legítimo de tooling, no ruido ni cambio a medio terminar

**Fecha:** 2026-09-09
**Estado:** Comiteado (`chore(mobile): sync iOS Xcode project after icon/splash regeneration`)

**Contexto:** tras el trabajo de app icon/splash screen de las Decisiones 45 (commits `d3a0a1a`, `db9714a`, `a1f1a00`), quedó un diff sin comitear en `mobile/ios/` (`Podfile`, `AppFrameworkInfo.plist`, `Runner.xcodeproj/project.pbxproj`, `Runner.xcscheme`, `Runner.xcworkspace`, `AppDelegate.swift`, más `Podfile.lock` y `swiftpm/Package.resolved` sin trackear).

**Decisión:** verificar el diff en vez de asumir que era ruido o un cambio roto, y comitearlo si resultaba coherente. Se confirmó que es exactamente el artefacto esperado de correr `pod install`/`flutter build ios` con Flutter 3.47.2: wiring de CocoaPods (`Pods_Runner.framework`, `Pods.xcodeproj` agregado al workspace) + migración de Flutter a Swift Package Manager (`FlutterGeneratedPluginSwiftPackage`) + el patrón nuevo `FlutterImplicitEngineDelegate` para el registro del engine en `AppDelegate.swift`. Sin secretos, sin placeholders, sin ediciones a medias — se comiteó completo.

**Por qué esto importa como aprendizaje:** en este working tree compartido, cuando se re-corre tooling de Flutter después de tocar assets nativos (íconos/splash), regenera en silencio un diff grande y alarmante en `mobile/ios/` que a simple vista parece un cambio ajeno o roto, pero es boilerplate legítimo. Vale la pena diffear los archivos clave (`Podfile`, `AppDelegate.swift`, `Info.plist`) antes de descartarlo como ruido o, al revés, de asumir que está roto.
